import{V as e,_ as t,p as n,u as r}from"./runtime-core.esm-bundler-CXb3iQhv.js";var i={class:`markdown-body`},a={__name:`isometric`,setup(a,{expose:o}){return o({frontmatter:{}}),(a,o)=>(e(),n(`div`,i,[...o[0]||=[t(`<h1 id="isometric%20flow%20debugging" tabindex="-1">Isometric Flow Debugging</h1><p>This page diagnoses logical coordinates, projection, Regions, fixed Ports, and routing in Isometric Viewer and Isometric Editor. Always preserve the boundary between business <code>{u,v,z}</code> and screen <code>{x,y}</code>.</p><h2 id="document%20and%20coordinate%20input" tabindex="-1">Document and coordinate input</h2><h3 id="minimal%20codec%20check" tabindex="-1">Minimal Codec check</h3><p>Use <code>createIsometricDocumentCodec()</code> to isolate input. Record <code>kind</code>, <code>schemaVersion</code>, node, Region, edge, and waypoint counts, then verify Results and diagnostics from <code>create()</code>, <code>decode()</code>, and <code>encode()</code>.</p><p>Separate structural failure from module absence. Invalid <code>{u,v,z}</code>, duplicate ids, and invalid references belong to Codec. An absent Renderer module preserves original node data and produces MissingNode Projection.</p><h3 id="logical%20coordinates" tabindex="-1">Logical coordinates</h3><p>For node-position issues, capture:</p><table><thead><tr><th>Field</th><th>Meaning</th><th>Must not be confused with</th></tr></thead><tbody><tr><td><code>u</code>, <code>v</code></td><td>Logical position on the isometric plane</td><td>screen x, y</td></tr><tr><td><code>z</code></td><td>Logical height or layer position</td><td>CSS <code>z-index</code></td></tr><tr><td><code>footprint</code></td><td>Logical area occupied by a node or Region</td><td>DOM width and height</td></tr><tr><td>elevation/height</td><td>Model height and projected thickness</td><td>viewport zoom</td></tr></tbody></table><p>Screen drag is inverse-projected before a Command writes logical coordinates. Never write projected positions into GraphDocument.</p><h3 id="route%20document" tabindex="-1">Route document</h3><p>An edge stores stable endpoints, fixed Ports, and normalized manual-route data only. Projected Ports, candidate routes, hit polygons, and Router intermediate results belong to Runtime or Session.</p><h3 id="preserve%20state%20on%20invalid%20replacement" tabindex="-1">Preserve state on invalid replacement</h3><p>Compare document fingerprint, revision, Selection, and route session across replacement. A failed candidate must not contaminate the current GraphDocument or editing session.</p><h2 id="projection%20chain%20and%20surface%20bounds" tabindex="-1">Projection chain and surface bounds</h2><h3 id="projection%20input%20and%20output" tabindex="-1">Projection input and output</h3><p>Capture the complete chain for one node:</p>`,17),r(`div`,{class:`code-wrapper`,"data-lang":`text`},[r(`button`,{class:`tui-icon ti-copy`,title:`Copy Code`,onclick:`copyCode(this)`},[r(`span`,{class:`lang-name`},`text`)]),r(`pre`,{class:`language-text`},[r(`code`,{class:`language-text`},`logical {u,v,z} + footprint
  → Coordinate capability
  → projected position / polygon
  → depth
  → canvas position
  → viewport
`)])],-1),t(`<p>Assign every mismatch to document, coordinate capability, projection descriptor, Surface, or viewport.</p><h3 id="depth%20ordering" tabindex="-1">Depth ordering</h3><p>Depth comes from product projection and semantic layers. It is not node-array order or CSS <code>z-index</code>. Verify Region, Edge, Node, Edge Label, Port, and interaction previews remain in their role bands.</p><h3 id="surface%20bounds" tabindex="-1">Surface bounds</h3><p>Complete bounds include node footprint, model height, Region, edge path, Marker, and Port ranges required by fit. When fit clips content, find the missing Projection in aggregation first.</p><h3 id="viewport%20operations" tabindex="-1">Viewport operations</h3><p>Capture <code>{x,y,zoom}</code> before and after <code>fitView()</code>, <code>fitSelection()</code>, <code>centerView()</code>, zoom, and <code>setViewport()</code>. Viewport operations never modify <code>{u,v,z}</code> or create document History.</p><h2 id="regions%20and%20render%20ordering" tabindex="-1">Regions and render ordering</h2><h3 id="region%20hierarchy" tabindex="-1">Region hierarchy</h3><p>Inspect Region role, parent relation, logical footprint, elevation, and child ownership. A missing parent or invalid relation is reported by relationship validation, not repaired by Renderer guesses.</p><h3 id="region%20elevation" tabindex="-1">Region elevation</h3><p>Region elevation affects top surface, sides, child baseline, and complete bounds. Record logical height, projected polygon, and final depth rather than screen coordinates only.</p><h3 id="node%20renderer" tabindex="-1">Node Renderer</h3><p>Trace node type → Renderer Contribution → owner module → Effective Model → Isometric descriptor. Graphic, Material, Application, and custom nodes retain module ownership; the public component has no type branches.</p><h3 id="hit%20testing%20and%20visual%20order" tabindex="-1">Hit testing and visual order</h3><p>Visual order alone does not prove correct hit testing. Capture projected polygon, node host, Port layer, viewport, and <code>elementFromPoint</code> result. Do not use <code>pointer-events</code> as a patch for projection errors.</p><h2 id="fixed%20ports%20and%20port%20policy" tabindex="-1">Fixed Ports and Port Policy</h2><h3 id="eight%20fixed%20ports" tabindex="-1">Eight fixed Ports</h3><p>Confirm Port ids come from current Isometric constants and the node module permits them. Port positions derive from footprint, model dimensions, and Projection, not independent screen coordinates.</p><h3 id="logical%20directions" tabindex="-1">Logical directions</h3><p>Port direction uses logical <code>{u,v}</code> deltas. Projection rotates and skews visual directions, so screen angles must never decide north, south, east, or west.</p><h3 id="port%20projection" tabindex="-1">Port Projection</h3><p>Capture node logical coordinates, footprint, Port id, logical direction, projected point, hit area, and viewport. If display is correct but connection fails, continue through the hit layer and Connection Controller.</p><h3 id="port%20editing" tabindex="-1">Port editing</h3><p><code>startPortEdit()</code>, <code>resetNodePort()</code>, and <code>endPortEdit()</code> are public editing entries, but internal writes still go through Port Position Module and Command. Cancellation writes no document; module removal clears active editing state.</p><h2 id="automatic%20and%20manual%20routing" tabindex="-1">Automatic and manual routing</h2><h3 id="automatic-route%20input" tabindex="-1">Automatic-route input</h3><p>Record source and target footprints, Ports, obstacles, route rules, and fixed endpoint exit directions. Router input is logical data, never the current screen polyline alone.</p><h3 id="automatic-route%20result" tabindex="-1">Automatic-route Result</h3><p>Compare Result, logical route, projected route, and Renderer consumption. Unavailable or failed compute preserves the original edge and reports a diagnostic; it never silently chooses another algorithm.</p><h3 id="manual%20waypoints" tabindex="-1">Manual waypoints</h3><p>Capture waypoint id, logical position, segment Selection, active route session, Command, and Transaction. Candidate waypoints remain in Session until completion writes a normalized route.</p><h3 id="endpoint%20exit%20directions" tabindex="-1">Endpoint exit directions</h3><p>Fixed exit direction is a hard constraint. A shorter path cannot leave in the wrong direction. Inspect Router input, obstacles, or waypoints instead of modifying the first or last rendered segment.</p><h3 id="route%20authoring%20lifecycle" tabindex="-1">Route Authoring lifecycle</h3><p>Route Authoring is an independent Isometric module. The default Editor may install it as a locked Preset entry; <code>preset: false</code> does not. Uninstall or destroy clears candidates, subscriptions, Locale, and interaction sessions.</p><h2 id="editor%20projected%20interaction" tabindex="-1">Editor projected interaction</h2><h3 id="projected%20drag" tabindex="-1">Projected drag</h3><p>Capture pointer input, inverse-projection result, logical grid snap, Session preview, and final <code>{u,v,z}</code>. One completed gesture commits at most one Transaction.</p><h3 id="footprint%20resize" tabindex="-1">Footprint resize</h3><p>Resize commits footprint or model dimensions, not DOM width and height. No-op, Escape, pointer cancellation, and readonly cancellation leave document and History unchanged.</p><h3 id="fixed-port%20connection" tabindex="-1">Fixed-Port connection</h3><p>Inspect Port hit, endpoint role, Port Policy, Connection Controller, and final edge document. Candidate connections live in Session; only valid completion creates Command and Transaction.</p><h3 id="route%20editing" tabindex="-1">Route editing</h3><p>Waypoint add, move, remove, and cancel have distinct actions. Each completed action creates one Transaction; Selection and segment hover stay outside the document.</p><h3 id="readonly%20and%20stale%20commits" tabindex="-1">readonly and stale commits</h3><p>Readonly transitions cancel projected drag, Port connection, Port Position, and Route Authoring together. Any asynchronous candidate crossing <code>editEpoch</code> is rejected even if editable state returns later.</p><h2 id="module%20and%20resource%20lifecycle" tabindex="-1">Module and resource lifecycle</h2><h3 id="viewer%20modules" tabindex="-1">Viewer modules</h3><p>Verify install, removal, and reinstall for Background, Controls, Info Bar, and MiniMap. Their UI and Contributions move together while logical document and projection core remain available.</p><h3 id="node%20and%20region%20modules" tabindex="-1">Node and Region modules</h3><p>Use a Graphic, Material, Region, or Application module to verify Template, Renderer, Schema, assets, and Locale leave together. The document remains available while missing; reinstall restores Projection.</p><h3 id="routing%20modules" tabindex="-1">Routing modules</h3><p>Verify owners and dependencies for Port Position, Route Authoring, and Router separately. Removing manual-authoring UI must not break Viewer rendering of an existing manual route.</p><h3 id="multiple%20instances%20and%20products" tabindex="-1">Multiple instances and products</h3><p>Two Isometric instances never share Selection, Route Session, History, or Registry. Standard and Isometric on one page cannot install each other’s private capabilities or inspect each other’s protocol fields.</p><h2 id="focused%20validation" tabindex="-1">Focused validation</h2><h3 id="document%20and%20projection" tabindex="-1">Document and Projection</h3><table><thead><tr><th>Scope</th><th>Current entry</th></tr></thead><tbody><tr><td>State ownership</td><td><code>flow-iso-3-state-ownership.test.mjs</code></td></tr><tr><td>Codec pipeline</td><td><code>flow-iso-3-codec-pipeline.test.mjs</code></td></tr><tr><td>Effective Model</td><td><code>flow-iso-3-effective-model.test.mjs</code></td></tr><tr><td>Projection pipeline</td><td><code>flow-iso-3-projection-pipeline.test.mjs</code></td></tr><tr><td>Region</td><td><code>flow-iso-3-region-contract.test.mjs</code></td></tr></tbody></table><h3 id="ports%2C%20routing%2C%20and%20interaction" tabindex="-1">Ports, routing, and interaction</h3><table><thead><tr><th>Scope</th><th>Current entry</th></tr></thead><tbody><tr><td>Port Policy</td><td><code>flow-iso-3-port-policy.test.mjs</code></td></tr><tr><td>Router Preview</td><td><code>flow-iso-3-router-preview.test.mjs</code></td></tr><tr><td>Manual route</td><td><code>flow-iso-manual-route-authoring.test.mjs</code></td></tr><tr><td>Drag and resize</td><td><code>flow-iso-6-drag.test.mjs</code>, <code>flow-iso-6-resize.test.mjs</code></td></tr><tr><td>Connection</td><td><code>flow-iso-6-connection.test.mjs</code></td></tr><tr><td>Readonly and History</td><td><code>flow-iso-6-readonly-history.test.mjs</code></td></tr></tbody></table><h3 id="frame%20and%20completion%20criteria" tabindex="-1">Frame and completion criteria</h3><p>The full editor entry is <code>/#/frame/isoFlowEditorDemo</code>. With browser authorization, validate real projection, hits, depth, Ports, and route interaction. Record source, unit-contract, Frame, and browser evidence separately; no Standard result can replace an Isometric conclusion.</p>`,63)]]))}};export{a as default};