import{K as e,V as t,_ as n,p as r,u as i,v as a,y as o}from"./runtime-core.esm-bundler-CXb3iQhv.js";var s={class:`markdown-body`},c={__name:`lightbox`,setup(c,{expose:l}){return l({frontmatter:{}}),(c,l)=>{let u=e(`DocTag`),d=e(`DocComponentDependencies`),f=e(`DemoLightboxBasic`),p=e(`DemoLightboxGallery`),m=e(`DemoLightboxToolbar`);return t(),r(`div`,s,[l[0]||=i(`h1`,{id:`lightbox`,tabindex:`-1`},`Lightbox`,-1),o(u,{types:`new inov exp`,ver:`0.1.2`}),l[1]||=i(`p`,null,[i(`code`,null,`TuiLightbox`),a(` is a global overlay component for image viewing. It uses `),i(`code`,null,`TuiInxViewport`),a(` internally, so images can be panned, wheel-zoomed, control-zoomed, pinch-zoomed on touch screens, rotated, reset, switched, and downloaded.`)],-1),l[2]||=i(`p`,null,`The component is scoped to lightbox-style image viewing. Video preview is not part of the first version, and gesture-based swipe switching is intentionally deferred because it conflicts with image panning.`,-1),l[3]||=i(`h2`,{id:`component%20relationships`,tabindex:`-1`},`Component Relationships`,-1),o(d,{"component-name":`TuiLightbox`}),l[4]||=i(`h2`,{id:`basic%20preview`,tabindex:`-1`},`Basic Preview`,-1),l[5]||=i(`p`,null,[a(`The basic usage controls the layer with `),i(`code`,null,`v-model`),a(` and passes a single image through `),i(`code`,null,`src`),a(`.`)],-1),o(f),l[6]||=i(`h2`,{id:`gallery%20preview`,tabindex:`-1`},`Gallery Preview`,-1),l[7]||=i(`p`,null,[a(`Gallery usage passes `),i(`code`,null,`images`),a(` and controls the current item through `),i(`code`,null,`activeIndex`),a(` or `),i(`code`,null,`v-model:active-index`),a(`. Loop navigation and neighbor preloading are enabled by default.`)],-1),o(p),l[8]||=n(`<h2 id="toolbar%20config" tabindex="-1">Toolbar Config</h2><p><code>toolbar</code> is a single object for toolbar visibility, layout, placement, and default item configuration. The default actions use a <code>TuiIconButtonGroup</code> with <code>type=&quot;invert&quot;</code> and <code>TuiIconButton</code>; the positioning container stays transparent while the icon-button group owns the inverted semantic surface. The default toolbar is layered over the image viewport; in full bar mode, it reserves an inner <code>fitInset</code> safe area so the initial image does not start under the toolbar. The default toolbar includes an advanced view entry that opens a lightweight <code>TuiPopPanel</code> settings panel for switching <code>fit</code>, <code>bounds</code>, and preset combinations. Use the <code>toolbar</code> slot when the entire toolbar needs to be replaced.</p>`,2),o(m),l[9]||=n(`<h2 id="viewport%20notes" tabindex="-1">Viewport Notes</h2><p><code>TuiLightbox</code> is powered by <code>TuiInxViewport</code>. Once an image is open, users can pan, wheel zoom, control-button zoom, and pinch zoom on touch screens. After the image loads, its natural size is passed to the viewport so initial centering and fitting can be computed reliably.</p><p>Use <code>viewportConfig</code> to pass additional options to the internal <code>TuiInxViewport</code>. Top-level props such as <code>fit</code>, <code>bounds</code>, <code>minScale</code>, <code>maxScale</code>, <code>wheelZoom</code>, <code>pinchZoom</code>, <code>pinchRotate</code>, and <code>rotatable</code> override the same keys from <code>viewportConfig</code>.</p><h2 id="api%20reference" tabindex="-1">API Reference</h2><h3 id="props" tabindex="-1">Props</h3><table><thead><tr><th>Prop</th><th>Description</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><strong>Basic</strong></td><td></td><td></td><td></td></tr><tr><td><strong>modelValue</strong></td><td>Visibility of the lightbox layer. Supports <code>v-model</code></td><td><code>Boolean</code></td><td><code>false</code></td></tr><tr><td><strong>src</strong></td><td>Single image URL</td><td><code>String</code></td><td>-</td></tr><tr><td><strong>images</strong></td><td>Image list. Supports string arrays or object arrays</td><td><code>Array</code></td><td><code>[]</code></td></tr><tr><td><strong>activeIndex</strong></td><td>Current image index. Supports <code>v-model:active-index</code></td><td><code>Number</code></td><td>-</td></tr><tr><td><strong>keyMap</strong></td><td>Field mapping for image objects</td><td><code>Object</code></td><td><code>{}</code></td></tr><tr><td><strong>appendTo</strong></td><td>Teleport target selector or DOM element. Falls back to <code>$optFloaterTo</code>, then <code>#tuiRoot</code></td><td><code>String / HTMLElement</code></td><td>-</td></tr><tr><td><strong>zIndex</strong></td><td>Layer z-index. The backdrop uses this value, and the content layer uses this value plus 1</td><td><code>Number</code></td><td><code>2001</code></td></tr><tr><td><strong>Visibility &amp; Close</strong></td><td></td><td></td><td></td></tr><tr><td><strong>modal</strong></td><td>Whether to render a backdrop</td><td><code>Boolean</code></td><td><code>true</code></td></tr><tr><td><strong>lockScroll</strong></td><td>Whether to lock body scrolling while open</td><td><code>Boolean</code></td><td><code>true</code></td></tr><tr><td><strong>closeOnEsc</strong></td><td>Whether global Esc closes the layer</td><td><code>Boolean</code></td><td><code>true</code></td></tr><tr><td><strong>closeOnBackdrop</strong></td><td>Whether clicking the backdrop or empty body closes the layer</td><td><code>Boolean</code></td><td><code>true</code></td></tr><tr><td><strong>destroyOnClose</strong></td><td>Whether to destroy layer content after close</td><td><code>Boolean</code></td><td><code>true</code></td></tr><tr><td><strong>Image &amp; Viewport</strong></td><td></td><td></td><td></td></tr><tr><td><strong>viewportConfig</strong></td><td>Config passed to the internal <code>TuiInxViewport</code></td><td><code>Object</code></td><td><code>{}</code></td></tr><tr><td><strong>fit</strong></td><td>Initial fit mode: <code>none</code>, <code>contain</code>, <code>cover</code>, <code>width</code>, or <code>height</code></td><td><code>String</code></td><td><code>contain</code></td></tr><tr><td><strong>bounds</strong></td><td>Drag bounds mode: <code>none</code>, <code>viewport</code>, <code>content</code>, or <code>cover</code></td><td><code>String</code></td><td><code>viewport</code></td></tr><tr><td><strong>minScale</strong></td><td>Minimum scale</td><td><code>Number</code></td><td><code>0.2</code></td></tr><tr><td><strong>maxScale</strong></td><td>Maximum scale</td><td><code>Number</code></td><td><code>8</code></td></tr><tr><td><strong>wheelZoom</strong></td><td>Whether wheel zoom is enabled</td><td><code>Boolean</code></td><td><code>true</code></td></tr><tr><td><strong>pinchZoom</strong></td><td>Whether touch-screen pinch zoom is enabled</td><td><code>Boolean</code></td><td><code>true</code></td></tr><tr><td><strong>pinchRotate</strong></td><td>Whether touch-screen pinch rotate is enabled</td><td><code>Boolean</code></td><td><code>false</code></td></tr><tr><td><strong>rotatable</strong></td><td>Whether rotation controls and rotation behavior are enabled</td><td><code>Boolean</code></td><td><code>true</code></td></tr><tr><td><strong>resetOnImageChange</strong></td><td>Whether to reset the viewport when the image changes</td><td><code>Boolean</code></td><td><code>true</code></td></tr><tr><td><strong>showViewportControls</strong></td><td>Whether to show the inner <code>TuiInxViewport</code> control bar</td><td><code>Boolean</code></td><td><code>false</code></td></tr><tr><td><strong>debug</strong></td><td>Whether to emit lightbox and inner-viewport debug events for inspecting <code>fitInset</code>, natural image size, toolbar safe area, and viewport state</td><td><code>Boolean</code></td><td><code>false</code></td></tr><tr><td><strong>Gallery Behavior</strong></td><td></td><td></td><td></td></tr><tr><td><strong>loop</strong></td><td>Whether previous/next navigation loops</td><td><code>Boolean</code></td><td><code>true</code></td></tr><tr><td><strong>preload</strong></td><td>Whether adjacent images are preloaded</td><td><code>Boolean</code></td><td><code>true</code></td></tr><tr><td><strong>Toolbar</strong></td><td></td><td></td><td></td></tr><tr><td><strong>toolbar</strong></td><td>Toolbar config object for visibility, layout, placement, title, counter, and default items</td><td><code>Object</code></td><td><code>{}</code></td></tr><tr><td><strong>title</strong></td><td>Layer title. Falls back to the current image <code>title</code></td><td><code>String</code></td><td>-</td></tr><tr><td><strong>labels</strong></td><td>Local label overrides</td><td><code>Object</code></td><td>-</td></tr><tr><td><strong>Class Names</strong></td><td></td><td></td><td></td></tr><tr><td><strong>className</strong></td><td>Custom class for the layer</td><td><code>String | Array | Object</code></td><td>-</td></tr><tr><td><strong>contentClassName</strong></td><td>Custom class for the content body</td><td><code>String | Array | Object</code></td><td>-</td></tr><tr><td><strong>imageClassName</strong></td><td>Custom class for the image</td><td><code>String | Array | Object</code></td><td>-</td></tr><tr><td><strong>transitionName</strong></td><td>Transition name for the layer</td><td><code>String</code></td><td><code>fade</code></td></tr><tr><td><strong>imageTransitionName</strong></td><td>Transition name for image switching</td><td><code>String</code></td><td><code>tui-lbox-switch-fade</code></td></tr></tbody></table><h3 id="image%20object%20fields" tabindex="-1">Image Object Fields</h3><table><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><strong>src</strong></td><td>Image URL</td></tr><tr><td><strong>thumb</strong></td><td>Thumbnail URL. The component does not render it directly, but external thumbnail lists can reuse it</td></tr><tr><td><strong>title</strong></td><td>Image title</td></tr><tr><td><strong>alt</strong></td><td>Image alt text</td></tr><tr><td><strong>description</strong></td><td>Image description. Not rendered by the component itself</td></tr><tr><td><strong>downloadUrl</strong></td><td>Download URL. Falls back to <code>src</code></td></tr></tbody></table><h3 id="toolbar%20object" tabindex="-1">Toolbar Object</h3><table><thead><tr><th>Field</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><strong>show</strong></td><td>Whether to show the toolbar</td><td><code>true</code></td></tr><tr><td><strong>layout</strong></td><td>Toolbar layout: <code>bar</code>, <code>group</code>, or <code>split</code></td><td><code>bar</code></td></tr><tr><td><strong>placement</strong></td><td>Primary toolbar placement: <code>top</code> or <code>bottom</code></td><td><code>top</code></td></tr><tr><td><strong>offset</strong></td><td>Image safe area passed to inner <code>TuiInxViewport.fitInset</code>; supports number or array. Arrays follow CSS padding semantics with 2 / 3 / 4 values, such as <code>[vertical, horizontal]</code> or <code>[top, right, bottom, left]</code>. <code>bar</code> additionally adds the toolbar height to the toolbar side; <code>group</code> / <code>split</code> use the value as-is</td><td><code>20</code></td></tr><tr><td><strong>shift</strong></td><td>Visual toolbar shift for <code>group</code> / <code>split</code>; supports number or <code>[x, y]</code>. Ignored by <code>bar</code></td><td><code>0</code></td></tr><tr><td><strong>splitAlign</strong></td><td>Title/actions alignment in <code>split</code>: <code>center</code>, <code>edge</code>, or <code>reverse</code></td><td><code>center</code></td></tr><tr><td><strong>items</strong></td><td>Default toolbar item list. Supports strings or config objects. Single-image lightbox views hide <code>prev</code> / <code>next</code> automatically</td><td>Full default items</td></tr><tr><td><strong>buttonConfig</strong></td><td>Shared button config passed through the default toolbar <code>TuiIconButtonGroup</code>; all inner controls use <code>TuiIconButton</code>. The group has fixed <code>invert</code> semantics, so child button <code>type</code> values do not affect visual resolution</td><td><code>{}</code></td></tr><tr><td><strong>closeButtonConfig</strong></td><td><code>TuiIconButton</code> config for the close control</td><td><code>{}</code></td></tr><tr><td><strong>showCounter</strong></td><td>Whether to show the counter for multi-image lightbox viewing</td><td><code>true</code></td></tr><tr><td><strong>showTitle</strong></td><td>Whether to show the title</td><td><code>true</code></td></tr></tbody></table><h3 id="advanced%20view%20presets" tabindex="-1">Advanced View Presets</h3><p>The default toolbar item <code>advanced</code> opens the advanced view panel. Presets are only shortcuts; <code>fit</code> and <code>bounds</code> can still be adjusted independently.</p><table><thead><tr><th>Preset</th><th>fit</th><th>bounds</th><th>Description</th></tr></thead><tbody><tr><td><strong>Full view</strong></td><td><code>contain</code></td><td><code>viewport</code></td><td>Show the whole image and keep bounded panning</td></tr><tr><td><strong>Fill without blank</strong></td><td><code>cover</code></td><td><code>cover</code></td><td>Fill the viewport and avoid blank edges as much as possible</td></tr><tr><td><strong>Free drag</strong></td><td><code>contain</code></td><td><code>none</code></td><td>Show the whole image and disable drag bounds</td></tr><tr><td><strong>Fit width</strong></td><td><code>width</code></td><td><code>viewport</code></td><td>Fit the image to the viewport width</td></tr></tbody></table><h3 id="slots" tabindex="-1">Slots</h3><table><thead><tr><th>Slot</th><th>Description</th></tr></thead><tbody><tr><td><strong>title</strong></td><td>Custom title area. Slot props: <code>{ item, index }</code></td></tr><tr><td><strong>toolbar</strong></td><td>Custom toolbar. Slot props include current image, index, default buttons, action methods, current <code>fit</code> / <code>bounds</code>, and <code>setViewMode</code></td></tr><tr><td><strong>image</strong></td><td>Custom image content. Slot props: <code>{ item, index, loading, viewport }</code></td></tr><tr><td><strong>loading</strong></td><td>Custom loading state. By default, image loading renders <code>TuiResult status=&quot;loading&quot;</code></td></tr><tr><td><strong>error</strong></td><td>Custom error state. By default, image load failures render <code>TuiResult status=&quot;warning&quot;</code></td></tr><tr><td><strong>empty</strong></td><td>Custom empty state. By default, missing images render <code>TuiResult status=&quot;empty&quot;</code></td></tr></tbody></table><h3 id="events" tabindex="-1">Events</h3><table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><strong>update:modelValue</strong></td><td>Triggered when the preview opens or closes</td></tr><tr><td><strong>update:activeIndex</strong></td><td>Triggered when the current image index changes</td></tr><tr><td><strong>update:fit / update:bounds</strong></td><td>Triggered when the advanced view panel changes the fit or bounds mode</td></tr><tr><td><strong>open / opened</strong></td><td>Triggered when opening starts / after the first opened render</td></tr><tr><td><strong>close / closed</strong></td><td>Triggered when closing starts / after the leave transition finishes</td></tr><tr><td><strong>load</strong></td><td>Triggered when the image loads. Payload includes <code>index</code>, <code>item</code>, <code>naturalSize</code>, and the native event</td></tr><tr><td><strong>error</strong></td><td>Triggered when the image fails to load</td></tr><tr><td><strong>change</strong></td><td>Triggered when the current image changes. Payload includes <code>index</code>, <code>item</code>, and <code>source</code></td></tr><tr><td><strong>download</strong></td><td>Triggered when the default download button is clicked</td></tr><tr><td><strong>viewport-change</strong></td><td>Triggered when the internal viewport changes</td></tr><tr><td><strong>drag-start / drag / drag-end</strong></td><td>Internal viewport drag events</td></tr><tr><td><strong>zoom / rotate</strong></td><td>Internal viewport zoom or rotate events</td></tr><tr><td><strong>gesture-start / gesture / gesture-end</strong></td><td>Internal viewport touch gesture events</td></tr><tr><td><strong>reset / fit / debug</strong></td><td>Internal viewport reset, fit, and debug events; when <code>debug</code> is enabled, lightbox debug snapshots are emitted as well</td></tr><tr><td><strong>view-mode-change</strong></td><td>Triggered when the advanced view panel changes preset, <code>fit</code>, or <code>bounds</code></td></tr></tbody></table><h3 id="expose" tabindex="-1">Expose</h3><table><thead><tr><th>Name</th><th>Description</th><th>Type</th></tr></thead><tbody><tr><td><strong>open</strong></td><td>Open the layer manually. An image index can be passed</td><td><code>(index?: number) =&gt; void</code></td></tr><tr><td><strong>close</strong></td><td>Close the layer manually</td><td><code>() =&gt; void</code></td></tr><tr><td><strong>prev</strong></td><td>Switch to previous image</td><td><code>() =&gt; void</code></td></tr><tr><td><strong>next</strong></td><td>Switch to next image</td><td><code>() =&gt; void</code></td></tr><tr><td><strong>goTo</strong></td><td>Switch to a specific index</td><td><code>(index: number) =&gt; void</code></td></tr><tr><td><strong>download</strong></td><td>Download the current image</td><td><code>() =&gt; void</code></td></tr><tr><td><strong>reset</strong></td><td>Reset the internal viewport</td><td><code>() =&gt; void</code></td></tr><tr><td><strong>zoomIn / zoomOut</strong></td><td>Zoom in / out the internal viewport</td><td><code>() =&gt; void</code></td></tr><tr><td><strong>rotateLeft / rotateRight</strong></td><td>Rotate the internal viewport left / right</td><td><code>() =&gt; void</code></td></tr><tr><td><strong>fitToContent</strong></td><td>Refit the current image</td><td><code>() =&gt; void</code></td></tr><tr><td><strong>getVisible</strong></td><td>Get current visibility</td><td><code>() =&gt; boolean</code></td></tr><tr><td><strong>getActiveItem</strong></td><td>Get current image object</td><td><code>() =&gt; object</code></td></tr><tr><td><strong>getViewport</strong></td><td>Get the latest internal viewport state</td><td><code>() =&gt; object</code></td></tr></tbody></table><h3 id="css%20variables" tabindex="-1">CSS Variables</h3><table><thead><tr><th>Variable</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>--tui-lbox-toolbar-h</code></td><td>Toolbar height</td><td><code>48px</code></td></tr><tr><td><code>--tui-lbox-toolbar-gap</code></td><td>Toolbar inner gap</td><td><code>12px</code></td></tr><tr><td><code>--tui-lbox-motion-duration</code></td><td>Enter / leave animation duration</td><td><code>.7s</code></td></tr><tr><td><code>--tui-lbox-switch-duration</code></td><td>Image switching fade duration</td><td><code>.7s</code></td></tr><tr><td><code>--tui-lbox-motion-ease</code></td><td>Enter / leave animation easing</td><td><code>cubic-bezier(.22, .61, .36, 1)</code></td></tr><tr><td><code>--tui-lbox-toolbar-shift-x</code></td><td>Horizontal toolbar shift for <code>group</code> / <code>split</code></td><td><code>0px</code></td></tr><tr><td><code>--tui-lbox-toolbar-shift-y</code></td><td>Vertical toolbar shift for <code>group</code> / <code>split</code></td><td><code>0px</code></td></tr><tr><td><code>--tui-lbox-z</code></td><td>Content layer z-index</td><td><code>2002</code></td></tr><tr><td><code>--tui-inxvp-*</code></td><td>Internal <code>TuiInxViewport</code> variable family</td><td>-</td></tr></tbody></table>`,21)])}}};export{c as default};