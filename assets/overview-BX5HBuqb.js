import{c as o,l as a,aA as n,g as e}from"./index-CrE7RJmu.js";const s={class:"markdown-body"},p={__name:"overview",setup(r,{expose:i}){return i({frontmatter:{}}),(c,t)=>(o(),a("div",s,[...t[0]||(t[0]=[n('<h1 id="flow%20debugging%20%26%20state%20inspection" tabindex="-1">Flow Debugging &amp; State Inspection</h1><p>This protected section is for TechUI Flow maintainers. It covers source-level diagnosis, internal contracts, and acceptance evidence. Public Flow documentation describes stable APIs and application-side error handling only and must not link here.</p><h2 id="debugging%20boundaries" tabindex="-1">Debugging boundaries</h2><h3 id="identify%20the%20product%20first" tabindex="-1">Identify the product first</h3><p>Record four dimensions before investigating:</p><table><thead><tr><th>Dimension</th><th>Values</th><th>Why it matters</th></tr></thead><tbody><tr><td>Product</td><td>Standard, Isometric</td><td>Selects the document, coordinate, and routing contracts</td></tr><tr><td>Component</td><td>Viewer, Editor</td><td>Determines whether editing sessions, Commands, and History apply</td></tr><tr><td>Assembly</td><td>Default Preset, <code>preset: false</code>, external modules</td><td>Identifies the installed capability set</td></tr><tr><td>Environment</td><td>Unit test, Frame, browser, production host</td><td>Defines what the evidence can prove</td></tr></tbody></table><p>A Standard result cannot prove Isometric behavior. An editing capability absent from Viewer must be marked not applicable instead of fabricated as passed.</p><h3 id="assign%20an%20owner%20layer" tabindex="-1">Assign an owner layer</h3><p>Assign one primary owner, then inspect adjacent links:</p>',9),e("div",{class:"code-wrapper","data-lang":"text"},[e("button",{class:"tui-icon ti-copy",title:"Copy Code",onclick:"copyCode(this)"},[e("span",{class:"lang-name"},"text")]),e("pre",{class:"language-text"},[e("code",{class:"language-text"},`input document
  → Codec
  → Document Store
  → Effective Model
  → Runtime Projection
  → Renderer / Surface
`)])],-1),e("p",null,"Editor adds the write chain:",-1),e("div",{class:"code-wrapper","data-lang":"text"},[e("button",{class:"tui-icon ti-copy",title:"Copy Code",onclick:"copyCode(this)"},[e("span",{class:"lang-name"},"text")]),e("pre",{class:"language-text"},[e("code",{class:"language-text"},`UI / Expose
  → Command
  → Transaction
  → canonical GraphDocument
  → History / Persistence subscribers
`)])],-1),n('<p>Do not patch Renderer, DOM, or add synchronization watchers before the owning layer is established.</p><h3 id="evidence%20boundaries" tabindex="-1">Evidence boundaries</h3><ul><li>Source inspection proves an implementation path, not browser interaction.</li><li>Unit tests prove input/output contracts, not real dimensions or hit testing.</li><li>A Frame proves that a full assembly entry exists; it is not browser acceptance by itself.</li><li>Browser evidence may be collected only with authorization for the current task.</li><li>Legacy comparison, production host, and development substitutes are separate evidence classes.</li></ul><h2 id="collection%20order" tabindex="-1">Collection order</h2><h3 id="step%201%3A%20freeze%20the%20input" tabindex="-1">Step 1: freeze the input</h3><p>Capture the smallest deterministic reproduction:</p><ul><li><code>kind</code>, <code>schemaVersion</code>, and document fingerprint;</li><li>node, region, edge, and waypoint counts;</li><li>component, Props, Preset, and external modules;</li><li>locale, readonly state, and container dimensions;</li><li>exact actions before the failure.</li></ul><p>Random node ids, current time, animation frames, and uncleared local storage must not be the only reproduction inputs.</p><h3 id="step%202%3A%20inspect%20the%20direct%20result" tabindex="-1">Step 2: inspect the direct Result</h3><p>Read the direct Result before events or derived state:</p><table><thead><tr><th>Result</th><th>Fields to retain</th></tr></thead><tbody><tr><td>Document</td><td><code>ok</code>, <code>value</code>, <code>error.code</code>, <code>diagnostics</code></td></tr><tr><td>Command</td><td><code>ok</code>, <code>committed</code>, <code>error.code</code>, <code>transaction</code></td></tr><tr><td>Module</td><td>module key, installed state, lock state, failure reason</td></tr><tr><td>Viewport</td><td><code>{x,y,zoom}</code> and complete content bounds</td></tr></tbody></table><p>When a failed Result has no <code>value</code>, never reuse an earlier value or candidate to simulate success.</p><h3 id="step%203%3A%20compare%20revisions" tabindex="-1">Step 3: compare revisions</h3><p>Record Document, Registry, and Session revisions separately:</p><ul><li>a document write advances only Document revision;</li><li>module installation or removal advances Registry revision;</li><li>Selection, hover, and interaction previews remain in Session;</li><li>one successful user edit produces one business Transaction.</li></ul><p>Do not collapse these domains into a generic “component updated” conclusion.</p><h3 id="step%204%3A%20verify%20cleanup" tabindex="-1">Step 4: verify cleanup</h3><p>After reproduction, destroy modules, Runtime, and page hosts. Listeners, timers, caches, storage connections, DOM references, and other disposers must return to their expected balance.</p><h2 id="state-domain%20inspection" tabindex="-1">State-domain inspection</h2><h3 id="graphdocument" tabindex="-1">GraphDocument</h3><p>GraphDocument is the business document shared by Viewer, Editor, and persistence. Compare canonical documents only:</p><ul><li>the root protocol, nodes, and edges remain JSON-compatible;</li><li>Selection, hover, projection coordinates, DOM measurements, and History do not enter the document;</li><li>an invalid replacement preserves the previous valid document;</li><li>missing node modules preserve their original node payload.</li></ul><h3 id="designersettings" tabindex="-1">designerSettings</h3><p><code>designerSettings</code> stores editor-only preferences such as panel layout and interaction preferences. It does not enter GraphDocument or graph-element History.</p><p>For UI-setting issues, compare the input Prop, <code>update:designerSettings</code>, and persistence restore result instead of observing the final panel position only.</p><h3 id="sessionstate" tabindex="-1">sessionState</h3><p>Selection, hover, active Command, drag preview, route candidate, and temporary form drafts belong to Session. Cancellation, readonly transitions, and Runtime destruction clear them according to contract.</p><h3 id="moduleruntimestate" tabindex="-1">moduleRuntimeState</h3><p>Module caches, subscriptions, resources, and Adapter connections belong to one Module Instance. Uninstall must remove its Contributions, capabilities, locale resources, and background listeners.</p><h3 id="runtime%20projection" tabindex="-1">Runtime Projection</h3><p>Runtime Projection is a read-only display result for Renderer and Surface. It may contain computed coordinates, effective styling, and fallbacks, but it is neither a GraphDocument write target nor an export format.</p><h2 id="module%2C%20capability%2C%20and%20contribution%20inspection" tabindex="-1">Module, Capability, and Contribution inspection</h2><h3 id="module%20installation" tabindex="-1">Module installation</h3><p>Investigate install failures in this order:</p><ol><li><code>productScope</code> matches the product.</li><li><code>activation</code> permits the requested lifecycle action.</li><li>required dependencies exist and conflicts are absent.</li><li>capability cardinality permits another provider.</li><li><code>setup()</code> did not fail or throw.</li><li>a failed Resource Scope rolled back completely.</li></ol><p>A Module Definition may be shared, but each Flow instance creates its own Module Instance.</p><h3 id="capability%20inspection" tabindex="-1">Capability inspection</h3><p>Record capability key, provider, cardinality, and consumers. An <code>exactly-one</code> conflict must not be silently resolved by registration order; a missing optional capability follows an explicit degraded path.</p><h3 id="contribution%20inspection" tabindex="-1">Contribution inspection</h3><p>For templates, Renderers, Schema, panels, toolbar actions, assets, and locales, record:</p><ul><li>contribution group and stable key;</li><li>owner module;</li><li>order, replacement policy, and availability conditions;</li><li>Registry revision before install, after install, and after uninstall;</li><li>whether UI, caches, and listeners leave with the Contribution.</li></ul><p>For MissingNode, trace node type → Renderer Contribution → owner module. Do not convert the node to a regular type as a shortcut.</p><h3 id="locks%20and%20uninstall" tabindex="-1">Locks and uninstall</h3><p>Locks come from Preset installation entries, not Module Definitions. When public uninstall returns <code>module-locked</code>, Registry revision, Contributions, and resources stay unchanged. Runtime destruction must still release every module resource.</p><h2 id="command%2C%20transaction%2C%20and%20history" tabindex="-1">Command, Transaction, and History</h2><h3 id="command%20availability" tabindex="-1">Command availability</h3><p>Toolbar, shortcuts, panels, and public methods for the same action share one Command. Compare <code>canExecute</code> and <code>execute</code> against the same readonly, missing-capability, input-validation, and <code>editEpoch</code> gates.</p><h3 id="transaction%20atomicity" tabindex="-1">Transaction atomicity</h3><p>A successful Transaction contains complete <code>before</code>, <code>after</code>, <code>changes</code>, and <code>revision</code>. Failure, cancellation, no-op, or stale asynchronous work must:</p><ul><li>leave GraphDocument unchanged;</li><li>publish no partial document;</li><li>add no History entry;</li><li>trigger no persistence save.</li></ul><p>Drag, resize, connect, and route authoring keep previews in Session and commit once at completion.</p><h3 id="history" tabindex="-1">History</h3><p>History subscribes only to committed Transactions. Record stack length, cursor, snapshot count, and document fingerprint:</p><ul><li>delta creation failure: the business document remains committed but no History entry is added;</li><li>undo/redo apply failure: document and cursor remain unchanged;</li><li>History uninstall: Commands and Transactions keep working while listeners and UI leave;</li><li>external replacement: rebase or clear History according to product contract without mixing old-document entries.</li></ul><h3 id="persistence" tabindex="-1">Persistence</h3><p>Use isolated scopes for every product and instance. Verify <code>save</code>, <code>load</code>, <code>clear</code>, and <code>dispose</code> counts, failure behavior, restore confirmation, and cross-document isolation. Never persist Selection or Runtime Projection.</p><h2 id="resource%20lifecycle" tabindex="-1">Resource lifecycle</h2><h3 id="install%20and%20disposal%20balance" tabindex="-1">Install and disposal balance</h3><p>Every module-owned resource enters Resource Scope. Track at least:</p><table><thead><tr><th>Resource</th><th>After install</th><th>After uninstall or destroy</th></tr></thead><tbody><tr><td>Event subscriptions</td><td>Increase with the module</td><td>Return to the previous count</td></tr><tr><td>Timers and animation frames</td><td>Exist only while active</td><td>Fully cancelled</td></tr><tr><td>Caches and transient state</td><td>Created on demand</td><td>Fully released</td></tr><tr><td>Storage connections</td><td>Exist while Adapter is active</td><td><code>dispose</code> called</td></tr><tr><td>DOM references</td><td>Exist while Surface is mounted</td><td>No longer retained by modules</td></tr></tbody></table><h3 id="multi-instance%20isolation" tabindex="-1">Multi-instance isolation</h3><p>Mount two instances of the same product and independently change Locale, Selection, History, modules, and persistence. Registry revision, document, and resources of one instance must not affect the other.</p><h3 id="issue%20record%20template" tabindex="-1">Issue record template</h3><p>Every issue record includes environment, route, product, component, Preset, fixture, reproduction steps, expected result, actual result, direct Result, state revisions, owning layer, executed evidence, and not-run checks. Unexecuted browser or production-host validation must be marked explicitly.</p>',64)])]))}};export{p as default};
