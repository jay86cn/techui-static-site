import{r as i,c as d,l as r,g as t,n,j as s,aA as c}from"./index-CrE7RJmu.js";const l={class:"markdown-body"},u={__name:"interaction",setup(h,{expose:o}){return o({frontmatter:{}}),(g,e)=>{const a=i("DocLinkItem");return d(),r("div",l,[e[0]||(e[0]=t("h1",{id:"tuifloweditor%20editing%20interactions",tabindex:"-1"},"TuiFlowEditor Editing Interactions",-1)),e[1]||(e[1]=t("p",null,[n("This page explains selection, drag, resize, and connection behavior in the Standard Editor. See "),t("a",{href:"./basicUsage"},"Basic Usage"),n(" for first steps and "),t("a",{href:"./programmaticControl"},"Programmatic Control"),n(" for business-code operations.")],-1)),s(a,{emoji:"🔗",label:"Standard FlowEditor",detail:"Open the full-size Editor example",link:"/#/frame/flowEditorDemo"}),e[2]||(e[2]=c('<h2 id="interaction%20fundamentals" tabindex="-1">Interaction fundamentals</h2><h3 id="preview%20and%20commit" tabindex="-1">Preview and commit</h3><p>Node drag, resize, and connection gestures first display a temporary preview. The Editor commits only after the user completes the operation.</p><p>One complete drag therefore produces one meaningful History entry instead of one entry for every pointer position.</p><h3 id="coordinate%20rules" tabindex="-1">Coordinate rules</h3><p>Standard FlowEditor uses <code>{x,y}</code> plane coordinates. Canvas pan and zoom change display position without changing document coordinates.</p><p>Applications and plugins must not derive node coordinates from DOM positions. Use <a href="./programmaticControl">Programmatic Control</a> for code-driven movement.</p><h2 id="selection" tabindex="-1">Selection</h2><h3 id="click%20selection" tabindex="-1">Click selection</h3><p>Click a node or edge to make it the primary target. Click the background to clear selection. Hold <code>Ctrl/Cmd</code> while clicking to add a node or edge; click it again with the modifier to remove it.</p><p>The primary target controls the contextual properties, while the full selection controls batch move, copy, deletion, and arrangement.</p><p>Press <code>Ctrl/Cmd + A</code> to select all selectable nodes. Press <code>Esc</code> to clear selection when no gesture is active.</p><h3 id="selection%20box" tabindex="-1">Selection box</h3><p>Hold <code>Shift</code> and drag from the canvas background to select several nodes. Hold <code>Ctrl/Cmd + Shift</code> while drawing the box to add its nodes to the existing selection; <code>Shift</code> alone uses the new box result.</p><p>Selection affects the current editing session only and does not enter History.</p><h3 id="multiple-selection%20limits" tabindex="-1">Multiple-selection limits</h3><p>Hidden, locked, or type-restricted targets may not participate in every batch Action. The Toolbar enables Actions according to selection count and target capabilities.</p><h3 id="hover%20feedback" tabindex="-1">Hover feedback</h3><p>In the idle state, a Handle follows the semantic color of its connected edge. When several edges share a Handle, the edge with the highest z-index supplies the color; the later painted edge wins when z-index values are equal. Edge layers are ordered as default &lt; hover-related &lt; selected, so a selected edge always keeps the final semantic color at a shared Handle. Hovering a node emphasizes its Handles and every incident edge. Hovering an edge emphasizes only that edge and its endpoint Handles. Default edges use the primary color, while explicitly semantic edges and endpoints retain their semantic color. Dashed, dotted, and custom patterns animate while related hover is active.</p><p>A selected node hides its connection Handles and keeps only the selection outline and resize controls. Handles reappear according to connection rules when a connection gesture starts.</p><h2 id="node%20drag" tabindex="-1">Node drag</h2><h3 id="drag%20one%20node" tabindex="-1">Drag one node</h3><p>Dragging shows the target location; release commits the new <code>{x,y}</code> coordinates.</p><p>Press <code>Esc</code> during the drag to cancel immediately. Cancellation, locking, or a readonly transition preserves the document from before the drag.</p><h3 id="drag%20multiple%20nodes" tabindex="-1">Drag multiple nodes</h3><p>After multiple selection, drag one node to move every movable selected node while retaining relative positions. Restricted nodes are not force-written.</p><h3 id="grid%20snapping" tabindex="-1">Grid snapping</h3><p>When snapping is enabled, pointer and arrow-key movement use <code>snapGrid</code>. With snapping disabled, an arrow key moves one coordinate unit. <code>Shift + Arrow key</code> multiplies the active step by four.</p><h3 id="near%20the%20canvas%20edge" tabindex="-1">Near the canvas edge</h3><p>Dragging near an edge can adjust the viewport according to interaction settings. Viewport movement and node coordinates remain separate; only document coordinates are saved.</p><h2 id="node%20resize" tabindex="-1">Node resize</h2><h3 id="resize%20handles" tabindex="-1">Resize handles</h3><p>Selecting a resizable node reveals eight resize controls at the four corners and four edge midpoints. They can change width and height and can also change position when resizing from the top or left.</p><p>Press <code>Esc</code> during resize to discard the preview and retain the previous size and position.</p><h3 id="size%20constraints" tabindex="-1">Size constraints</h3><p>Node types and Designer settings can limit:</p><ul><li>whether resizing is available;</li><li>minimum and maximum dimensions;</li><li>aspect ratio;</li><li>allowed resize directions.</li></ul><p>Values outside the constraints are not written.</p><h3 id="multiple%20selection%20and%20locking" tabindex="-1">Multiple selection and locking</h3><p>Resize applies to the primary node, not every selected node. Locked nodes and readonly Editors do not expose an available resize operation.</p><h2 id="edge%20creation" tabindex="-1">Edge creation</h2><h3 id="fixed%20handles" tabindex="-1">Fixed Handles</h3><p>Standard nodes commonly use <code>top</code>, <code>right</code>, <code>bottom</code>, and <code>left</code> Handles. Node type and shared configuration determine visibility, connectability, and appearance.</p><h3 id="drag%20connection" tabindex="-1">Drag connection</h3><p>Drag from an available Handle to a target Handle. The path remains a preview until drop.</p><h3 id="click%20connection" tabindex="-1">Click connection</h3><p>Clicking a Handle starts manual connection authoring without selecting its node. Click the source, optionally click the background repeatedly to add path anchors, and then click the target Handle. Clicking the target directly creates a manual edge with no authored anchor. Press <code>Backspace</code> to remove the latest anchor; click the source again or press <code>Esc</code> to cancel.</p><p>Manual connection keeps the authored source, target, and anchors fixed instead of switching endpoints automatically. Step, smooth-step, and status routes can extend each segment in only one of four directions: up, right, down, or left. The pointer expresses direction and distance; the rendered terminal is its projection onto the currently allowed axis and does not have to coincide with the pointer. Clicking the background or a Region stores that rendered terminal rather than the raw click position, and the next segment uses the perpendicular axis.</p><p>Step and smooth-step modes add a derived dashed guide during free routing. Only the active-axis delta grows or shrinks by the minimum grid unit relative to the latest Handle or authored anchor; the fixed axis remains exactly equal to the previous point, so the anchor itself does not have to align with the canvas’s absolute grid. The perpendicular dashed guide runs from the primary terminal toward the pointer. Clicking free space stores that preview terminal directly, without a second absolute-grid snap, and the guide cannot create an anchor. When a valid target Handle is reached, the primary terminal temporarily drops final-segment delta snapping and moves to the exact orthogonal closure point, and the guide points exactly to the target. After completion, both segments render as the configured route, with smooth-step retaining its rounded closure corner. Neither the guide nor its closure point is persisted as a route anchor. Status routes do not use this guide behavior. Straight and Bezier modes keep the terminal at the pointer and retain their original click behavior.</p><p>Once connection authoring starts, Regions behave as transparent canvas and cannot become selected or dragged. Handles on other nodes become passive targets: they may complete the current connection but can never restart authoring from themselves. Completion is bound to the active connection session, so stale or duplicate clicks cannot start a second connection.</p><p>A node can never connect to itself. Every Handle on the source node is an invalid target, including the source Handle and every other Handle on that node. They do not participate in target snapping and cannot commit an edge. This rule applies to click-authored manual connections, drag-authored automatic connections, and endpoint reconnection.</p><p>When the pointer reaches a self-connection, a Handle that the current status single-direction segment cannot reach, a role conflict, a locked target, or any other invalid target, the entire preview path—from the source through every authored anchor to the constrained terminal—uses the danger color. The Step and smooth-step derived guides must turn red as well; coloring only the final segment or target Handle is insufficient. Moving away from the invalid target restores the normal preview. This danger state is transient and never enters edge styling or the Flow document. A free pointer that has not reached any target remains pending rather than invalid.</p><h3 id="strict%20and%20loose%20modes" tabindex="-1">Strict and loose modes</h3><p>Strict mode distinguishes source and target roles and rejects invalid direction. Loose mode can choose compatible Handles from the two nodes.</p><p>Both modes require two different nodes and reject incomplete edges.</p><p>One undirected node pair can keep at most one automatic edge. Dragging a second automatic edge reports that the same two nodes can have only one automatic edge and preserves the original document. Click-authored manual edges may add more connections for that pair without relaxing automatic-edge uniqueness.</p><p>After either endpoint node of a committed manual edge actually moves, the Editor converts every affected manual edge to <code>route: { mode: &#39;auto&#39;, waypoints: [] }</code> in the same document transaction, releases the hard lock on the former manual Handles, and reselects Handles and an automatic path from the new node positions. Unrelated manual edges remain unchanged; cancelling the drag or keeping the same position does not convert anything. If conversion would violate automatic-edge uniqueness for a node pair, the entire node move is rejected and both node positions and edges remain unchanged.</p><h2 id="endpoint%20changes" tabindex="-1">Endpoint changes</h2><h3 id="reconnect" tabindex="-1">Reconnect</h3><p>When endpoint editing is allowed, drag an existing endpoint to a new node or Handle. The Editor replaces it only after validation succeeds.</p><p>After selecting an editable edge, its ring endpoint controls are placed above the node Handles and share the same geometry endpoints as the rendered path. Default edges use the selected primary color, while explicitly semantic edges retain their semantic endpoint color.</p><p>Press <code>Esc</code> before reconnection completes to restore the original endpoint without leaving a partial edge.</p><h3 id="delete%20on%20detach" tabindex="-1">Delete on detach</h3><p>Some configuration allows an edge to be deleted when its endpoint is dragged away from every valid target. The interaction configuration determines whether and how far.</p><h3 id="edge%20content" tabindex="-1">Edge content</h3><p>Endpoints define the relationship. Labels and styles belong to edge content and can be changed without reconnecting.</p><h2 id="cancel%20interactions" tabindex="-1">Cancel interactions</h2><h3 id="use%20esc" tabindex="-1">Use Esc</h3><p><code>Esc</code> first cancels active Template DnD, node drag, resize, or connection. It clears selection only when no gesture is active.</p><h3 id="automatic%20cancellation" tabindex="-1">Automatic cancellation</h3><p>An interaction is cancelled when:</p><ul><li>the Editor becomes readonly;</li><li>the document is replaced;</li><li>a relevant node or module becomes unavailable;</li><li>the Editor is unmounted;</li><li>the pointer session becomes invalid.</li></ul><p>Cancellation discards only the current preview and does not undo earlier completed edits.</p><h2 id="readonly%20and%20locking" tabindex="-1">Readonly and locking</h2><h3 id="editor%20readonly" tabindex="-1">Editor readonly</h3><p>Readonly mode blocks every write gesture and cancels active editing while retaining pan, zoom, and viewing.</p><h3 id="node%20locking" tabindex="-1">Node locking</h3><p>Node locking limits movement, resize, and some edits for one node. Selection, focus, and content viewing can remain available where appropriate.</p><h3 id="late%20operations" tabindex="-1">Late operations</h3><p>If an asynchronous operation returns after readonly, document replacement, or target removal, it must not overwrite the newer state.</p><h2 id="interaction%20events" tabindex="-1">Interaction events</h2><h3 id="four%20phases" tabindex="-1">Four phases</h3><table><thead><tr><th>Event</th><th>Purpose</th></tr></thead><tbody><tr><td><code>interactionStart</code></td><td>An editing gesture starts.</td></tr><tr><td><code>interactionPreview</code></td><td>The gesture produces a temporary preview.</td></tr><tr><td><code>interactionEnd</code></td><td>The gesture completes with its final result.</td></tr><tr><td><code>interactionCancel</code></td><td>The gesture is cancelled.</td></tr></tbody></table><p>These events can drive surrounding status UI and analytics. Save from final document changes rather than preview events.</p><h3 id="selection%20event" tabindex="-1">Selection event</h3><p><code>selectionChange</code> fires when the selection set changes. Selection is not part of the Flow document and does not replace <code>v-model</code> or <code>change</code>.</p><h3 id="drag-and-drop%20events" tabindex="-1">Drag-and-drop events</h3><p><code>dndSession</code> describes Template DnD state, and <code>dropNode</code> fires after a successful canvas drop. Their payloads describe the current operation and must not be persisted as the Flow document.</p><p>See <a href="./reference#component-events">API Reference</a> for exact event arguments.</p><h2 id="common%20issues" tabindex="-1">Common issues</h2><table><thead><tr><th>Symptom</th><th>Common cause</th></tr></thead><tbody><tr><td>A node cannot be dragged</td><td>The Editor is readonly, the node is locked, or the type blocks movement.</td></tr><tr><td>Box selection pans the canvas</td><td><code>Shift</code> was not held, the gesture did not start on empty background, or selection configuration differs.</td></tr><tr><td>Resize handles are missing</td><td>The node is not selected, is locked, or does not support resize.</td></tr><tr><td>A connection cannot start</td><td>The Handle is unavailable or has the wrong role in strict mode.</td></tr><tr><td>A connection cannot land</td><td>The target is unavailable or endpoint directions conflict.</td></tr><tr><td>An edge disappears after detach</td><td>Delete-on-detach is enabled.</td></tr><tr><td>Shortcuts do not work</td><td>The Workbench is not focused or an input owns focus.</td></tr></tbody></table><h2 id="related%20documentation" tabindex="-1">Related documentation</h2><ul><li><a href="./basicUsage">Standard FlowEditor: Basic Usage</a></li><li><a href="./programmaticControl">Standard FlowEditor: Programmatic Control</a></li><li><a href="./advancedUsage">Standard FlowEditor: Advanced Usage</a></li><li><a href="./reference">Standard FlowEditor: API Reference</a></li></ul>',93))])}}};export{u as default};
