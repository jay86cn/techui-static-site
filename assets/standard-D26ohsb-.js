import{c as d,l as a,aA as t,g as e}from"./index-CrE7RJmu.js";const i={class:"markdown-body"},p={__name:"standard",setup(r,{expose:n}){return n({frontmatter:{}}),(c,o)=>(d(),a("div",i,[...o[0]||(o[0]=[t('<h1 id="standard%20flow%20debugging" tabindex="-1">Standard Flow Debugging</h1><p>This page diagnoses two-dimensional document, viewport, node, edge, and editing issues in Standard Viewer and Standard Editor. Every coordinate conclusion uses <code>{x,y}</code> and must not reuse Isometric projection or Port rules.</p><h2 id="document%20and%20codec" tabindex="-1">Document and Codec</h2><h3 id="build%20a%20minimal%20input" tabindex="-1">Build a minimal input</h3><p>Use <code>createStandardDocumentCodec()</code> before investigating component DOM:</p><ol><li>Verify <code>create()</code> produces a canonical document.</li><li>Verify <code>decode()</code> separates JSON, root-protocol, and element failures.</li><li>Verify <code>encode()</code> removes runtime fields and redundant overrides.</li><li>Repeat the same input and confirm deterministic output.</li></ol><p>Record at least <code>kind</code>, <code>schemaVersion</code>, node and edge counts, the Result, and diagnostic codes.</p><h3 id="validate%20nodes" tabindex="-1">Validate nodes</h3><p>Inspect nodes in this order:</p><table><thead><tr><th>Check</th><th>Expected</th><th>Owner on failure</th></tr></thead><tbody><tr><td><code>id</code></td><td>Unique string within the document</td><td>Codec structural validation</td></tr><tr><td><code>type</code></td><td>Stable type key</td><td>Codec or node module</td></tr><tr><td><code>position</code></td><td>Finite <code>{x,y}</code></td><td>Standard Codec</td></tr><tr><td><code>size</code></td><td>Normalizable size</td><td>Node-type cleaner</td></tr><tr><td>Parent relation</td><td>Existing parent and valid relation</td><td>Region/relationship validation</td></tr></tbody></table><p>A structurally valid node whose Renderer module is absent enters MissingNode projection while its original JSON remains canonical.</p><h3 id="validate%20edges" tabindex="-1">Validate edges</h3><p>Inspect source, target, fixed Handle, label, Marker, and line configuration together. Missing endpoints or invalid Handles produce stable diagnostics; Renderer must not rewrite endpoints to hide the failure.</p><h3 id="preserve%20state%20on%20invalid%20replacement" tabindex="-1">Preserve state on invalid replacement</h3><p>For a mounted Runtime, capture document fingerprint and revision before and after replacement. A failed Result preserves the current valid document, publishes no partial node set, and does not clear Selection to simulate recovery.</p><h2 id="effective%20model" tabindex="-1">Effective Model</h2><h3 id="identify%20the%20merge%20layer" tabindex="-1">Identify the merge layer</h3><p>Inspect Standard node and edge values across product defaults, module-contributed defaults, document common settings, type settings, and instance overrides. Assign every visual or size difference to one layer first.</p><h3 id="compare%20shared%20consumers" tabindex="-1">Compare shared consumers</h3><p>For the same node or edge, compare:</p><ul><li>Renderer Runtime Projection;</li><li>Inspector display value;</li><li>Command validation input;</li><li>sparse overrides before export.</li></ul><p>All consumers use one Effective Model. Do not add separate default-merging logic to Inspector or Renderer.</p><h3 id="clear%20overrides" tabindex="-1">Clear overrides</h3><p>After clearing an instance override, the element inherits the latest common value instead of copying that value into the node. Verify that <code>false</code>, <code>0</code>, empty string, and empty arrays are not treated as missing.</p><h3 id="diagnose%20module%20removal" tabindex="-1">Diagnose module removal</h3><p>Removing a node module removes its defaults, Schema, and Renderer together while preserving matching nodes and module data in the document. Reinstalling a new Module Instance rebuilds the Effective Model from original data.</p><h2 id="viewer%20and%20viewport" tabindex="-1">Viewer and viewport</h2><h3 id="container%20dimension%20chain" tabindex="-1">Container dimension chain</h3><p>Record actual width, height, and <code>min-width/min-height</code> from the component root through Standard Surface to the Vue Flow host. If a parent height is zero, fix the container contract before changing fit logic.</p><h3 id="viewport%20state" tabindex="-1">Viewport state</h3><p>Capture <code>{x,y,zoom}</code> before and after every operation:</p><table><thead><tr><th>Operation</th><th>Verify</th></tr></thead><tbody><tr><td><code>fitView()</code></td><td>Uses complete surface bounds and respects zoom limits</td></tr><tr><td><code>centerView()</code></td><td>Preserves the intended zoom and changes the center only</td></tr><tr><td><code>zoomIn()</code> / <code>zoomOut()</code></td><td>Uses controller steps and emits three-phase events</td></tr><tr><td><code>resetZoom()</code></td><td>Restores the intended zoom without changing document coordinates</td></tr><tr><td><code>setViewport()</code></td><td>Normalizes input and returns current viewport</td></tr></tbody></table><p>Viewport is runtime state. It does not rewrite node positions or create an editing Transaction.</p><h3 id="surface%20bounds" tabindex="-1">Surface bounds</h3><p>Capture node sizes, Region extents, edge paths, and aggregated bounds separately. When fit omits content, find the Projection missing from aggregation rather than adding fixed canvas padding.</p><h3 id="viewport%20events" tabindex="-1">Viewport events</h3><p>One continuous pan or zoom produces <code>viewportChangeStart</code>, zero or more <code>viewportChange</code>, and one <code>viewportChangeEnd</code>. High-frequency updates must not use Registry revision to rebuild the whole document Projection.</p><h2 id="renderer%2C%20nodes%2C%20and%20edges" tabindex="-1">Renderer, nodes, and edges</h2><h3 id="resolve%20a%20renderer" tabindex="-1">Resolve a Renderer</h3><p>Trace node presentation through:</p>',40),e("div",{class:"code-wrapper","data-lang":"text"},[e("button",{class:"tui-icon ti-copy",title:"Copy Code",onclick:"copyCode(this)"},[e("span",{class:"lang-name"},"text")]),e("pre",{class:"language-text"},[e("code",{class:"language-text"},`node.type
  → node renderer contribution
  → owner module
  → Effective Model
  → Runtime descriptor
  → Standard node host
`)])],-1),t('<p>Node-type branches belong to modules, not public components or generic Kernel code.</p><h3 id="handles%20and%20connection%20points" tabindex="-1">Handles and connection points</h3><p>Standard uses product-defined fixed Handles. Inspect Handle id, source/target role, visibility policy, hit radius, and connection mode. Never reuse Isometric Port Policy.</p><h3 id="edge%20geometry" tabindex="-1">Edge geometry</h3><p>Retain endpoint positions, Handles, path type, Marker, label, line styling, and geometry Result together. A geometry failure preserves the document edge and reports a diagnostic; Renderer does not create another business route.</p><h3 id="regions%20and%20layers" tabindex="-1">Regions and layers</h3><p>Inspect Region role, parent relation, local node position, and semantic layer. Reordering changes only local order within the owning role band and cannot raise Region above normal nodes or interaction layers.</p><h2 id="editor%2C%20selection%2C%20and%20interaction" tabindex="-1">Editor, Selection, and interaction</h2><h3 id="selection" tabindex="-1">Selection</h3><p>Capture selected node ids, edge ids, primary target, and Session revision. Single selection, modifier multi-selection, marquee, and clear are Session operations and produce no document Transaction or History entry.</p><h3 id="drag%20and%20resize" tabindex="-1">Drag and resize</h3><p>One gesture follows:</p>',12),e("div",{class:"code-wrapper","data-lang":"text"},[e("button",{class:"tui-icon ti-copy",title:"Copy Code",onclick:"copyCode(this)"},[e("span",{class:"lang-name"},"text")]),e("pre",{class:"language-text"},[e("code",{class:"language-text"},`pointer start
  → Session preview
  → pointer end
  → one Command
  → one Transaction
`)])],-1),t('<p>Inspect grid snapping, final-pointer flush, position or size changes, and Transaction count. Escape, pointer cancellation, and no-op completion write no document.</p><h3 id="create%20and%20update%20edges" tabindex="-1">Create and update edges</h3><p>Inspect Handle hit, connection validity, auto-pan, and endpoint updates. Create, update, or detach-delete commits at most one Transaction; invalid endpoints terminate at the Product Controller boundary.</p><h3 id="readonly%20and%20stale%20commits" tabindex="-1">readonly and stale commits</h3><p>Capture <code>editEpoch</code>, active interaction, and candidate document during a readonly transition. The transition cancels interaction immediately. An asynchronous Command that crossed the transition fails before commit even if editable state is later restored.</p><h3 id="keyboard%2C%20clipboard%2C%20and%20template%20drop" tabindex="-1">Keyboard, clipboard, and template drop</h3><p>Shortcuts, toolbar actions, and public methods call the same Command. Clipboard stores product-local canonical fragments. Template-drop screen coordinates are used for one coordinate conversion and never become a second node-position truth.</p><h2 id="module%20and%20resource%20lifecycle" tabindex="-1">Module and resource lifecycle</h2><h3 id="viewer%20modules" tabindex="-1">Viewer modules</h3><p>Remove Background, Controls, MiniMap, and Info Bar one at a time. Their Contributions and UI leave while GraphDocument and unrelated Viewer capabilities stay unchanged. Reinstall creates a new Module Instance.</p><h3 id="editor%20modules" tabindex="-1">Editor modules</h3><p>Identify owners for Templates, Layers, History, JSON, Persistence, Inspector, Configuration, and workbench Contributions. Uninstall removes Controller, subscriptions, caches, Locale, and storage connections, not only the tab.</p><h3 id="node%20modules" tabindex="-1">Node modules</h3><p>Use Info, Image, or Icon as a sample. Template, Schema, Renderer, assets, Locale, and diagnostics install and uninstall together. Missing modules degrade display; reinstall restores the node from original data.</p><h3 id="multiple%20instances" tabindex="-1">Multiple instances</h3><p>Two Standard instances on one page own separate Runtime, Registry, Selection, History, Locale, and Persistence scopes. Sharing a Module Definition never means sharing active state.</p><h2 id="focused%20validation" tabindex="-1">Focused validation</h2><h3 id="recommended%20unit%20contracts" tabindex="-1">Recommended unit contracts</h3><p>Select the smallest set by owner:</p><table><thead><tr><th>Scope</th><th>Current entry</th></tr></thead><tbody><tr><td>Viewer Runtime</td><td><code>flow-viewer-runtime.test.mjs</code></td></tr><tr><td>Editor Runtime</td><td><code>flow-editor-runtime.test.mjs</code></td></tr><tr><td>History</td><td><code>flow-history-runtime.test.mjs</code></td></tr><tr><td>Module Host</td><td><code>flow-module-host.test.mjs</code></td></tr><tr><td>Standard Viewer product behavior</td><td><code>flow-u3-standard-viewer-parity.test.mjs</code></td></tr><tr><td>Standard Editor public contract</td><td><code>flow-u4-standard-editor-parity.test.mjs</code></td></tr><tr><td>Standard module workbench</td><td><code>flow-u5-standard-modules-parity.test.mjs</code></td></tr><tr><td>Module ownership</td><td><code>flow-u1-module-ownership.test.mjs</code></td></tr></tbody></table><p>Retired Standard probes must not be revived by restoring historical directories.</p><h3 id="frame%20and%20browser" tabindex="-1">Frame and browser</h3><p>The full editor entry is <code>/#/frame/flowEditorDemo</code>. Only with browser authorization should maintainers inspect real dimensions, wheel zoom, canvas drag, node hits, overlay focus, and the complete workbench.</p><h3 id="completion%20criteria" tabindex="-1">Completion criteria</h3><p>State separately whether source inspection, unit contracts, Frame, and browser checks ran. Codec success does not prove Renderer behavior, and Standard success does not prove Isometric behavior.</p>',25)])]))}};export{p as default};
