import{K as e,V as t,_ as n,p as r,u as i,v as a,y as o}from"./runtime-core.esm-bundler-CXb3iQhv.js";var s={class:`markdown-body`},c={__name:`svgInjector`,setup(c,{expose:l}){return l({frontmatter:{}}),(c,l)=>{let u=e(`DocTag`),d=e(`DocLinkItem`),f=e(`DemoSvgInjectorBasic`),p=e(`DemoSvgInjectorStyling`),m=e(`DemoSvgInjectorLoad`);return t(),r(`div`,s,[l[0]||=i(`h1`,{id:`svg%20injector`,tabindex:`-1`},`SVG Injector`,-1),o(u,{types:`star wrap`}),l[1]||=i(`p`,null,[a(`SVGInjector is a Vue component built on top of the open-source library `),i(`code`,null,`svg-injector`),a(`. It first renders an `),i(`code`,null,`<img>`),a(` element, and once loading is complete, replaces that element with a real inline `),i(`code`,null,`<svg>`),a(`. This makes external SVG files behave much more like locally inlined SVGs for styling purposes.`)],-1),l[2]||=i(`p`,null,[a(`This approach is especially useful for icons, illustrations, brand graphics, and similar resources, because the injected SVG can respond to CSS `),i(`code`,null,`fill`),a(`, sizing, opacity, and component state changes.`)],-1),o(d,{emoji:`🔗`,label:`TechUI SvgInjector`,detail:`Full Demo`,link:`/#/frame/svgInjectorDemo`}),l[3]||=i(`h2`,{id:`basic%20usage`,tabindex:`-1`},`Basic Usage`,-1),l[4]||=i(`h3`,{id:`basic%20injection`,tabindex:`-1`},`Basic Injection`,-1),l[5]||=i(`p`,null,[a(`Simply pass the SVG file path through the `),i(`code`,null,`src`),a(` prop, and the component will handle async loading and injection automatically.`)],-1),o(f),l[6]||=n(`<h3 id="style%20control" tabindex="-1">Style Control</h3><p>The newer version of the component directly provides props such as <code>width</code>, <code>height</code>, <code>padding</code>, and <code>color</code>, so common icon sizing and coloring can be handled through props without relying on extra class-based styles.</p><p>When <code>color</code> is provided, the component applies a unified fill color to the injected <code>svg</code> and its common shape nodes, which is suitable for most monochrome icon scenarios.</p>`,3),o(p),l[7]||=n(`<h2 id="loading%20and%20error%20handling" tabindex="-1">Loading and Error Handling</h2><p>SVG injection is inherently asynchronous, so the component internally maintains four states: <code>idle</code>, <code>loading</code>, <code>loaded</code>, and <code>error</code>, and exposes corresponding events.</p><h3 id="load%20complete" tabindex="-1">Load Complete</h3><p>When the SVG is injected successfully and replaces the original DOM node, the <code>load</code> event is emitted.</p>`,4),o(m),l[8]||=n('<h3 id="error%20fallback" tabindex="-1">Error Fallback</h3><p>When SVG loading fails, the <code>fallback</code> prop controls how the component degrades:</p><ul><li><code>img</code>: Keep the original <code>&lt;img&gt;</code> element as the fallback display.</li><li><code>hide</code>: Hide the component entirely on error.</li><li><code>slot</code>: Render the default slot content on error.</li></ul><p>This makes the component suitable not only for pure icon display, but also for business pages that need dedicated error fallback content.</p><h2 id="technical%20notes%20and%20limitations" tabindex="-1">Technical Notes and Limitations</h2><div class="custom-block custom-block-warning"><p class="custom-block-title">DOM Replacement Mechanism and `ref` Limitation</p><p>The component initially renders an <code>&lt;img&gt;</code>. After the SVG content loads successfully, the underlying library directly replaces that node with an inline <code>&lt;svg&gt;</code>.</p><p>Because of this, a template <code>ref</code> will consistently point to the original <code>img</code> before injection, not the final generated <code>svg</code>. If you need to operate on the real SVG DOM after injection, it is recommended to do so in the <code>load</code> callback or use a native DOM query to retrieve the final node.</p></div><div class="custom-block custom-block-tip"><p class="custom-block-title">Exposed State</p><p>The component exposes three reactive states through <code>defineExpose</code>: <code>isLoading</code>, <code>isLoaded</code>, and <code>isError</code>. Parent components can access these through a template ref for use with skeletons, error messaging, or business-state decisions.</p></div><h2 id="api%20reference" tabindex="-1">API Reference</h2><h3 id="props" tabindex="-1">Props</h3><table><thead><tr><th>Property</th><th>Description</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><strong>src</strong></td><td>SVG file path</td><td><code>String</code></td><td><code>&#39;&#39;</code></td></tr><tr><td><strong>alt</strong></td><td>Alt text for the pre-injection <code>&lt;img&gt;</code></td><td><code>String</code></td><td><code>&#39;&#39;</code></td></tr><tr><td><strong>options</strong></td><td>Advanced config object passed through to <code>svg-injector</code></td><td><code>Object</code></td><td><code>{}</code></td></tr><tr><td><strong>fallback</strong></td><td>Fallback strategy on load failure. Options: <code>img</code>, <code>hide</code>, <code>slot</code></td><td><code>String</code></td><td><code>&#39;img&#39;</code></td></tr><tr><td><strong>width</strong></td><td>Width of the injection area in px</td><td><code>Number</code></td><td>—</td></tr><tr><td><strong>height</strong></td><td>Height of the injection area in px</td><td><code>Number</code></td><td>—</td></tr><tr><td><strong>padding</strong></td><td>Padding of the container in px</td><td><code>Number</code></td><td>—</td></tr><tr><td><strong>color</strong></td><td>Unified fill color for the injected SVG</td><td><code>String</code></td><td>—</td></tr></tbody></table><h3 id="events" tabindex="-1">Events</h3><table><thead><tr><th>Event</th><th>Description</th><th>Callback</th></tr></thead><tbody><tr><td><strong>load</strong></td><td>Triggered after successful SVG injection</td><td>Injected node <code>Element</code></td></tr><tr><td><strong>error</strong></td><td>Triggered when SVG loading or injection fails</td><td>Error message <code>String</code></td></tr></tbody></table><h3 id="expose" tabindex="-1">Expose</h3><table><thead><tr><th>Exposed Item</th><th>Description</th><th>Type</th></tr></thead><tbody><tr><td><strong>isLoading</strong></td><td>Whether the component is currently loading</td><td><code>ComputedRef&lt;boolean&gt;</code></td></tr><tr><td><strong>isLoaded</strong></td><td>Whether the SVG has been injected successfully</td><td><code>ComputedRef&lt;boolean&gt;</code></td></tr><tr><td><strong>isError</strong></td><td>Whether the component is in error state</td><td><code>ComputedRef&lt;boolean&gt;</code></td></tr></tbody></table><h3 id="slots" tabindex="-1">Slots</h3><table><thead><tr><th>Slot</th><th>Description</th></tr></thead><tbody><tr><td><strong>default</strong></td><td>Fallback content shown when <code>fallback=&quot;slot&quot;</code> and loading fails</td></tr></tbody></table><h3 id="css%20variables" tabindex="-1">CSS Variables</h3><table><thead><tr><th>Variable</th><th>Description</th></tr></thead><tbody><tr><td><code>--tui-svginj-w</code></td><td>Effective width variable</td></tr><tr><td><code>--tui-svginj-h</code></td><td>Effective height variable</td></tr><tr><td><code>--tui-svginj-pd</code></td><td>Effective padding variable</td></tr><tr><td><code>--tui-svginj-color</code></td><td>Effective color variable</td></tr></tbody></table>',18)])}}};export{c as default};