import{V as e,_ as t,p as n}from"./runtime-core.esm-bundler-CXb3iQhv.js";var r={class:`markdown-body`},i={__name:`intro`,setup(i,{expose:a}){return a({frontmatter:{}}),(i,a)=>(e(),n(`div`,r,[...a[0]||=[t(`<h1 id="multilingual%20overview" tabindex="-1">Multilingual Overview</h1><p>The multilingual (Internationalization, or i18n for short) module of TechUI is a lightweight solution built on the global service <code>$tService</code> and an on-demand resource loading mechanism.</p><p>Unlike traditional i18n plugins that require loading all language packs at once, TechUI uses an <strong>on-demand asynchronous loading</strong> strategy. On startup, the system resolves the active language from persisted configuration, optional initialization parameters, and the current environment, then loads only the locale resources currently needed. This reduces the initial bundle size and improves application performance.</p><h2 id="core%20features" tabindex="-1">Core Features</h2><h3 id="auto%20detection%20and%20loading" tabindex="-1">Auto Detection and Loading</h3><p>The system includes built-in locale resolution logic. During initialization, the target language is determined in the following priority order:</p><ol><li><strong>Persisted configuration</strong>: First check whether <code>globalConfig.lang</code> already stores the user’s chosen locale.</li><li><strong>Initialization parameter</strong>: If <code>TechUIInit</code> explicitly provides <code>initLang</code>, and no persisted language exists yet, use that value next.</li><li><strong>System / browser locale</strong>: If neither a persisted language nor an initialization locale is available, infer the current environment locale through <code>tLang.getSysLang()</code>.</li><li><strong>Default fallback</strong>: If none of the above matches, fall back to Simplified Chinese (<code>zh-CN</code>).</li></ol><h3 id="asynchronous%20on-demand%20strategy" tabindex="-1">Asynchronous On-Demand Strategy</h3><p>TechUI locale packs are designed as independent JavaScript modules (located in <code>@techui/locales</code>). The system only loads the resources required by the current target language instead of preloading every language up front, which saves bandwidth and memory usage.</p><h3 id="modular%20management" tabindex="-1">Modular Management</h3><p>To prevent locale packs from becoming oversized and difficult to maintain as the project evolves, TechUI adopts a <strong>language module structure</strong>. For example, in the <code>lang/zh-CN</code> directory, translation files are split into:</p><ul><li><code>index.js</code>: Current locale registration entry.</li><li><code>common/index.js</code>: Aggregation entry for common vocabulary and base semantics (OK, Cancel, theme, state, etc.).</li><li><code>common/result.js</code>: Result state copy.</li><li><code>components/index.js</code>: Aggregation entry for standard and lightweight component copy (such as <code>i18n</code>, <code>image</code>, <code>navTab</code>, and <code>adaptive</code>).</li><li><code>components/lightbox.js</code>: Lightbox component copy.</li><li><code>components/controlPanel.js</code>: Control panel copy.</li><li><code>components/panicAlert.js</code>: Global error/alert copy.</li><li><code>components/login.js</code>: Login module text.</li><li><code>components/dateTime.js</code>: Date and time related text.</li><li><code>extended/index.js</code>: Aggregation entry for extended component copy (such as <code>chart</code> and <code>inxViewport</code>).</li><li><code>extended/dataUnit.js</code>: Extended DataUnit copy.</li></ul><p>During the final compilation, these modules are aggregated into a single language entry file (such as <code>lang/zh-CN/index.js</code>). This keeps development maintainable while preserving runtime loading efficiency.</p><h2 id="support%20range" tabindex="-1">Support Range</h2><p>TechUI officially provides built-in support for the following three standard locale environments:</p><table><thead><tr><th>Language Code</th><th>Description</th></tr></thead><tbody><tr><td><strong>zh-CN</strong></td><td>Simplified Chinese</td></tr><tr><td><strong>zh-TW</strong></td><td>Traditional Chinese</td></tr><tr><td><strong>en</strong></td><td>English</td></tr></tbody></table><h2 id="runtime%20switching" tabindex="-1">Runtime Switching</h2><p>TechUI now supports runtime language switching. When a user changes the language, the service updates <code>globalConfig.lang</code> and loads the target language resources on demand. Components that depend on reactive language state then refresh their text automatically. Because the locale value is persisted together with <code>globalConfig</code>, the user’s last choice remains the highest-priority source on the next startup.</p><p>Content that was converted into plain strings during initialization still needs to be refactored into reactive usage if it should update at runtime. Prefer calling <code>i18n(&#39;xxx&#39;)</code> directly in templates, or using <code>computed(() =&gt; i18n(&#39;xxx&#39;))</code> in scripts.</p>`,19)]]))}};export{i as default};