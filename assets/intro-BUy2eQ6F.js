import{V as e,_ as t,p as n}from"./runtime-core.esm-bundler-CXb3iQhv.js";var r={class:`markdown-body`},i={__name:`intro`,setup(i,{expose:a}){return a({frontmatter:{}}),(i,a)=>(e(),n(`div`,r,[...a[0]||=[t(`<h1 id="%E5%A4%9A%E8%AF%AD%E8%A8%80%E6%A6%82%E8%A7%88" tabindex="-1">多语言概览</h1><p>TechUI 的多语言（Internationalization，简称 i18n）模块是一套基于全局服务 <code>$tService</code> 和按需资源加载机制构建的轻量级解决方案。</p><p>与传统的 i18n 插件需要一次性加载所有语言包不同，TechUI 采用 <strong>按需异步加载</strong> 策略。系统会在启动时结合本地持久化配置、初始化参数与当前环境，仅请求并加载当前所需的语言资源，从而显著减少首屏资源体积，提升应用性能。</p><h2 id="%E6%A0%B8%E5%BF%83%E7%89%B9%E6%80%A7" tabindex="-1">核心特性</h2><h3 id="%E8%87%AA%E5%8A%A8%E4%BE%A6%E6%B5%8B%E4%B8%8E%E5%8A%A0%E8%BD%BD" tabindex="-1">自动侦测与加载</h3><p>系统内置了语言环境判断逻辑。在应用初始化阶段，系统会按照以下优先级确定目标语言：</p><ol><li><strong>本地持久化配置</strong>：优先检查 <code>globalConfig.lang</code> 中是否保存了用户手动切换过的语言偏好。</li><li><strong>初始化传参</strong>：如果在 <code>TechUIInit</code> 时显式传入了 <code>initLang</code>，且本地尚无持久化语言配置，则优先采用该值。</li><li><strong>系统/浏览器语言</strong>：若无本地配置且未传入初始化语言，则通过 <code>tLang.getSysLang()</code> 推导当前环境语言。</li><li><strong>默认兜底</strong>：如果上述均无法匹配，则默认加载简体中文 (<code>zh-CN</code>)。</li></ol><h3 id="%E5%BC%82%E6%AD%A5%E6%8C%89%E9%9C%80%E7%AD%96%E7%95%A5" tabindex="-1">异步按需策略</h3><p>TechUI 的语言包被设计为独立的 JavaScript 模块（位于 <code>@techui/locales</code>）。系统只会在需要时加载当前目标语言对应的资源，不会在首次启动时一次性加载全部语言内容，从而节省网络带宽和内存占用。</p><h3 id="%E6%A8%A1%E5%9D%97%E5%8C%96%E7%AE%A1%E7%90%86" tabindex="-1">模块化管理</h3><p>为了解决随着项目迭代导致语言包文件过大、难以维护的问题，TechUI 采用了 <strong>语言模块结构</strong>。 例如在 <code>lang/zh-CN</code> 目录下，翻译文件被拆分为：</p><ul><li><code>index.js</code>：当前语言注册入口。</li><li><code>common/index.js</code>：通用词汇与基础语义聚合入口（确定、取消、主题、状态等）。</li><li><code>common/result.js</code>：结果状态相关文案。</li><li><code>components/index.js</code>：普通组件与轻量组件文案聚合入口（如 <code>i18n</code>、<code>image</code>、<code>navTab</code>、<code>adaptive</code>）。</li><li><code>components/lightbox.js</code>：Lightbox 组件文案。</li><li><code>components/controlPanel.js</code>：控制面板相关文案。</li><li><code>components/panicAlert.js</code>：全局错误/告警相关文案。</li><li><code>components/login.js</code>：登录模块文案。</li><li><code>components/dateTime.js</code>：日期时间相关文案。</li><li><code>extended/index.js</code>：扩展组件文案聚合入口（如 <code>chart</code>、<code>inxViewport</code>）。</li><li><code>extended/dataUnit.js</code>：扩展 DataUnit 文案。</li></ul><p>在最终编译时，这些模块会被聚合为一个语言入口文件（如 <code>lang/zh-CN/index.js</code>），既保证了开发时的可维护性，又保证了运行时的加载效率。</p><h2 id="%E6%94%AF%E6%8C%81%E8%8C%83%E5%9B%B4" tabindex="-1">支持范围</h2><p>目前 TechUI 官方内置支持以下三种标准语言环境：</p><table><thead><tr><th style="text-align:left;">语言代码</th><th style="text-align:left;">描述</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>zh-CN</strong></td><td style="text-align:left;">简体中文 (Simplified Chinese)</td></tr><tr><td style="text-align:left;"><strong>zh-TW</strong></td><td style="text-align:left;">繁体中文 (Traditional Chinese)</td></tr><tr><td style="text-align:left;"><strong>en</strong></td><td style="text-align:left;">英语 (English)</td></tr></tbody></table><h2 id="%E8%BF%90%E8%A1%8C%E6%97%B6%E5%88%87%E6%8D%A2" tabindex="-1">运行时切换</h2><p>TechUI 当前支持运行时切换语言。 当用户切换语言后，service 会更新 <code>globalConfig.lang</code>，并按需加载对应语言资源；已使用响应式语言状态的组件会自动刷新对应文案。由于该值会随 <code>globalConfig</code> 一起持久化，因此用户的切换结果会在下次启动时继续生效，并拥有最高优先级。</p><p>对于在初始化阶段就被固化为普通字符串的内容，则仍需改造成响应式写法，才能在运行时跟随语言切换更新。推荐在模板中直接调用 <code>i18n(&#39;xxx&#39;)</code>，或在脚本中使用 <code>computed(() =&gt; i18n(&#39;xxx&#39;))</code>。</p>`,19)]]))}};export{i as default};