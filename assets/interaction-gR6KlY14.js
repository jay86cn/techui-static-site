import{r as i,c as d,l as r,g as t,n as o,j as s,aA as c}from"./index-CrE7RJmu.js";const l={class:"markdown-body"},g={__name:"interaction",setup(p,{expose:n}){return n({frontmatter:{}}),(u,e)=>{const a=i("DocLinkItem");return d(),r("div",l,[e[0]||(e[0]=t("h1",{id:"tuiisofloweditor%20editing%20interactions",tabindex:"-1"},"TuiIsoFlowEditor Editing Interactions",-1)),e[1]||(e[1]=t("p",null,[o("This page explains canvas selection, dragging, footprint resizing, fixed ports, and route authoring. Every direction is based on logical "),t("code",null,"{u,v}"),o(" axes, and the editor writes a result only after the user completes a valid operation.")],-1)),s(a,{emoji:"🔗",label:"Isometric FlowEditor",detail:"Open the full-size editor example",link:"/#/frame/isoFlowEditorDemo"}),e[2]||(e[2]=c('<h2 id="basic%20interaction%20rules" tabindex="-1">Basic Interaction Rules</h2><h3 id="preview%20and%20commit" tabindex="-1">Preview and commit</h3><p>Node drag, footprint resize, port connection, and route editing first display a temporary preview. The editor commits a new logical document only after pointer release and validation.</p><p>One complete gesture creates at most one history entry. No change, cancellation, or validation failure preserves the current document.</p><h3 id="logical%20coordinates" tabindex="-1">Logical coordinates</h3><p>The browser provides screen coordinates. The editor combines the viewport and isometric projection to convert them into <code>{u,v,z}</code>. The document stores logical coordinates, footprints, ports, and manual waypoints, not projected pixels, depth, or temporary route segments.</p><h2 id="selection" tabindex="-1">Selection</h2><h3 id="click%20and%20multiple%20selection" tabindex="-1">Click and multiple selection</h3><p>Selecting a node, region, or edge replaces the current selection. Hold <code>Shift</code> or <code>Ctrl/Cmd</code> to add or remove an element. Selecting empty canvas space clears the selection.</p><p><code>Ctrl/Cmd + A</code> selects every selectable element. Selection belongs to the current editor session and is not stored in the Flow document.</p><h3 id="box%20selection" tabindex="-1">Box selection</h3><p>Hold <code>Shift</code> and drag from empty canvas space to box-select nodes. Also hold <code>Ctrl/Cmd</code> to add matching nodes to the existing selection.</p><p>Box selection does not automatically select edges between those nodes. Use Layers when covered elements need precise selection.</p><h3 id="hover%20feedback" tabindex="-1">Hover feedback</h3><p>In the idle state, a Port follows the semantic color of its connected edge. When several edges share a Port, the edge with the highest z-index supplies the color; the later painted edge wins when z-index values are equal. Edge layers are ordered as default &lt; hover-related &lt; selected, so a selected edge always keeps the final semantic color at a shared Port. Hovering a node emphasizes its Ports and every incident edge. Hovering an edge emphasizes only that edge and its endpoint Ports. Default edges use the primary color, while explicitly semantic edges and Ports retain their semantic color. Dashed, dotted, and custom patterns animate while related hover is active.</p><p>A selected node hides connection Ports and keeps only its footprint outline and drag controls. Ports reappear according to connection rules when a connection gesture starts.</p><h2 id="drag%20nodes%20and%20regions" tabindex="-1">Drag Nodes and Regions</h2><h3 id="drag%20one%20element" tabindex="-1">Drag one element</h3><p>Drag an unlocked node or region to change its logical position. The canvas only previews the projection until release; then the editor commits <code>{u,v,z}</code>.</p><p>Normal canvas dragging keeps <code>z</code> unchanged. Arrow keys nudge selected nodes along logical axes, and <code>Shift + Arrow</code> uses a larger step.</p><h3 id="drag%20multiple%20nodes" tabindex="-1">Drag multiple nodes</h3><p>When one node in a selection is dragged, other movable nodes keep their relative positions and move with it. Hidden, locked, or non-movable nodes are excluded.</p><h3 id="snapping%20and%20canvas%20edges" tabindex="-1">Snapping and canvas edges</h3><p>With snapping enabled, the primary node lands on the configured logical step and other selected nodes use the same delta. During connection or route editing, approaching a canvas edge can pan the viewport. The document still stores only logical data.</p><h2 id="resize%20footprints" tabindex="-1">Resize Footprints</h2><h3 id="footprint%20handles" tabindex="-1">Footprint handles</h3><p>Select a resizable node or region and drag a projected edge or corner handle to update <code>footprint.u</code> and <code>footprint.v</code>. A node type can enforce a minimum footprint or disable resizing.</p><p>The operation does not start for a locked or hidden element, a read-only editor, or an unavailable projection.</p><h3 id="effect%20on%20ports%20and%20routes" tabindex="-1">Effect on ports and routes</h3><p>Port positions and connected routes are recalculated after a footprint change. Any actual footprint change to either endpoint node converts connected manual routes to automatic mode, so finish node layout and footprint changes before fine route authoring.</p><h2 id="create%20fixed-port%20connections" tabindex="-1">Create Fixed-Port Connections</h2><h3 id="eight%20logical%20directions" tabindex="-1">Eight logical directions</h3><p>Fixed ports are <code>u-</code>, <code>u+</code>, <code>v-</code>, <code>v+</code>, <code>u-v-</code>, <code>u+v-</code>, <code>u+v+</code>, and <code>u-v+</code>. They represent logical <code>{u,v}</code> deltas, not screen directions.</p><p>Regions do not expose regular connection ports. Node capability and effective configuration determine whether a node can connect and how its ports appear.</p><h3 id="drag%20or%20click%20to%20connect" tabindex="-1">Drag or click to connect</h3><p>Dragging from a source port to a target creates an automatic route. Selecting the source port, optional canvas waypoints, and target port in sequence creates a manual route. Candidate feedback shows whether the target is valid. The editor creates an edge only if port compatibility and route resolution succeed.</p><p>Press <code>Esc</code> to cancel a connection. When temporary waypoints exist, <code>Backspace</code> first removes the last waypoint.</p><h3 id="connection%20failure" tabindex="-1">Connection failure</h3><p>Self-connections, a second automatic edge for the same undirected node pair, locked nodes, invalid ports, and unresolved routes do not commit. A node pair may keep multiple manual edges. When an automatic edge already exists, the editor reports that the same two nodes can have only one automatic edge, preserves the original document, and never stores a one-ended edge.</p><h2 id="manual%20routes" tabindex="-1">Manual Routes</h2><h3 id="create%20a%20manual%20route" tabindex="-1">Create a manual route</h3><p>Clicking a source port starts a manual connection session. You may click empty canvas space to add logical waypoints, or directly select a target port that forms a valid straight segment with the source port. The committed edge stores <code>route.mode: &#39;manual&#39;</code>; straight routes use empty <code>waypoints</code>, while polylines store logical <code>{u,v,z}</code> values.</p><p>A manual route must still respect source exit direction, target entry direction, node avoidance, and no-backtracking rules. It is not an arbitrary screen polyline.</p><h3 id="view%20bends" tabindex="-1">View bends</h3><p>Hovering or selecting an automatic or manual route displays a small circle at every real logical turn. Bend colors follow the same state rules as the edge: default emphasis uses the primary color, while explicit semantics retain their semantic color. These circles describe route structure and are not drag handles. Collinear intermediate points are not displayed as bends, while endpoint handles keep their existing appearance and behavior.</p><p>After a manual route is committed, its endpoints, waypoints, and segments cannot be dragged, and double-clicking a segment does not insert a waypoint. To change the route, select it, press <code>Delete</code> to remove the complete edge, and connect it again.</p><h3 id="automatic%20versus%20manual" tabindex="-1">Automatic versus manual</h3><p>An automatic route stores only its mode; runtime computes the visible bends and recalculates them after node movement. A manual route stores logical waypoints for lines that must pass through chosen locations. Route mode is fixed when the connection session starts and cannot be switched in the Inspector after commit.</p><p>When either endpoint node of a committed manual edge actually moves, the Editor converts every connected manual edge to <code>route.mode: &#39;auto&#39;</code>, clears its <code>waypoints</code>, and resolves it from the new node position in the same document change. A cancelled drag or unchanged position does not convert the route. An actual footprint or port-geometry change follows the same rule. If conversion would conflict with an existing automatic edge for that node pair, the complete node change is rejected and the original document is preserved.</p><h2 id="reconnect%20edge%20endpoints" tabindex="-1">Reconnect Edge Endpoints</h2><h3 id="reconnect" tabindex="-1">Reconnect</h3><p>Select an editable edge and drag its source or target endpoint to another valid port. A successful operation uses the new endpoint and recalculates the route.</p><p>Press <code>Esc</code> or release over an invalid target to cancel and preserve the original edge.</p><h3 id="delete%20on%20detach" tabindex="-1">Delete on detach</h3><p>When delete-on-detach is enabled, dragging an endpoint away without dropping on a valid target can delete the entire edge. Connection settings control whether this behavior is enabled and the required distance.</p><h2 id="edit%20port%20positions" tabindex="-1">Edit Port Positions</h2><h3 id="enter%20port%20editing" tabindex="-1">Enter port editing</h3><p>Port positioning is an advanced capability in the default workbench. It adjusts logical anchors for fixed ports on a node footprint.</p><p>It is available only for a visible, unlocked node that supports fixed ports. Changing selection, entering read-only mode, or replacing the document cancels an unfinished port gesture.</p><h3 id="reset%20ports" tabindex="-1">Reset ports</h3><p>One port or all ports on a node can be restored to their default anchors. Connected routes are recalculated after a port change. Use the public methods in <a href="./programmaticControl">Programmatic Control</a> when application code must start or reset port editing.</p><h2 id="cancel%20interactions" tabindex="-1">Cancel Interactions</h2><h3 id="use%20esc" tabindex="-1">Use Esc</h3><p><code>Esc</code> first cancels an active drag, box selection, connection, endpoint, port, or route operation. When no operation is active, it clears selection.</p><p>Cancellation only removes uncommitted previews. Use history to undo an edit that has already completed.</p><h3 id="automatic%20cancellation" tabindex="-1">Automatic cancellation</h3><p>Entering read-only mode, replacing the document, losing the target element, uninstalling a required module, or destroying the component cancels the relevant interaction. Applications can also call <code>cancelInteraction()</code> before opening a dialog over the canvas or switching business documents.</p><h2 id="read-only%20and%20locking" tabindex="-1">Read-only and Locking</h2><h3 id="read-only%20editor" tabindex="-1">Read-only editor</h3><p>Read-only state blocks creation, dragging, footprint resizing, connections, port positioning, and route authoring while preserving inspection, pan, and zoom.</p><h3 id="element%20locking" tabindex="-1">Element locking</h3><p>A locked node cannot move, resize, edit ports, or be removed. A locked edge cannot change endpoints or routes. Element locks protect their target only and do not replace server-side permissions.</p><h2 id="interaction%20events" tabindex="-1">Interaction Events</h2><h3 id="four%20phases" tabindex="-1">Four phases</h3><table><thead><tr><th>Event</th><th>Meaning</th></tr></thead><tbody><tr><td><code>interactionStart</code></td><td>A canvas gesture starts.</td></tr><tr><td><code>interactionPreview</code></td><td>Temporary position, footprint, port, or route changes.</td></tr><tr><td><code>interactionEnd</code></td><td>The gesture ends with its final command result.</td></tr><tr><td><code>interactionCancel</code></td><td>The user or environment cancels the gesture.</td></tr></tbody></table><p>A preview does not mean the document changed. Do not save documents or send high-frequency requests from <code>interactionPreview</code>; use <code>change</code> or <code>v-model</code> for business persistence.</p><h3 id="selection%20and%20drop%20events" tabindex="-1">Selection and drop events</h3><p><code>selectionChange</code> synchronizes the current selection. <code>dropNode</code> and <code>dropRegion</code> report template-drop results. Use them to connect surrounding UI, not to mutate nested document objects directly.</p><h2 id="common%20issues" tabindex="-1">Common Issues</h2><table><thead><tr><th>Symptom</th><th>Common cause</th></tr></thead><tbody><tr><td>A node does not drag</td><td>The editor is read-only, or the node is locked or hidden.</td></tr><tr><td>Box selection pans the canvas</td><td>The gesture did not start on empty space, or <code>Shift</code> was not held.</td></tr><tr><td>Footprint handles do not appear</td><td>The node is not selected, is locked, or does not allow resizing.</td></tr><tr><td>Ports do not appear</td><td>The node is not connectable, display conditions are unmet, or the object is a region.</td></tr><tr><td>A connection cannot finish</td><td>Invalid target port, self-connection, duplicate connection, or unresolved route.</td></tr><tr><td>A manual-route bend does not drag</td><td>Manual geometry is read-only after commit; delete the complete edge and reconnect it.</td></tr><tr><td>A manual route becomes automatic after movement</td><td>This is intentional: moving either endpoint clears manual waypoints and restores automatic routing.</td></tr></tbody></table><h2 id="related%20documentation" tabindex="-1">Related Documentation</h2><ul><li><a href="./basicUsage">Isometric FlowEditor: Basic Usage</a></li><li><a href="./advancedUsage">Isometric FlowEditor: Advanced Usage</a></li><li><a href="./programmaticControl">Isometric FlowEditor: Programmatic Control</a></li><li><a href="./reference">Isometric FlowEditor: API Reference</a></li><li><a href="../isometric/documentModel">Isometric Flow: Document and Nodes</a></li><li><a href="../isometric/advancedUsage">Isometric Flow: Advanced Usage</a></li></ul>',82))])}}};export{g as default};
