import{_ as a,C as n,o as i,c as r,j as t,a as d,E as s,ah as l}from"./chunks/framework.PKn7PQPU.js";const y=JSON.parse('{"title":"Loader Panel","description":"","frontmatter":{},"headers":[],"relativePath":"en/tuiBase/dataDisplay/loaderPanel.md","filePath":"en/tuiBase/dataDisplay/loaderPanel.md"}'),c={name:"en/tuiBase/dataDisplay/loaderPanel.md"};function h(p,e,u,g,m,b){const o=n("AbsTag");return i(),r("div",null,[e[0]||(e[0]=t("h1",{id:"loader-panel",tabindex:"-1"},[d("Loader Panel "),t("a",{class:"header-anchor",href:"#loader-panel","aria-label":'Permalink to "Loader Panel"'},"​")],-1)),s(o,{types:"1st"}),e[1]||(e[1]=l('<p>A universal loading container used to wrap business modules. It not only provides unified visual feedback and interaction blocking for the loading state but also integrates the system&#39;s global initialization counting logic for coordinating page-level loading progress statistics.</p><p>Changes in the <code>inited</code> state of this component are automatically reported to the <code>portletCounter</code> in the global service. Through this mechanism, developers can control the global loading state of the <code>TuiAdaptive</code> component.</p><p><strong>Typical Example:</strong> If <code>portletCount</code> of <code>TuiAdaptive</code> is set to <code>5</code>, then when <strong>5 or more</strong> <code>ioaderPanel</code>s on the page enter the <code>inited</code> state, <code>TuiAdaptive</code> will automatically determine that global initialization is complete and stop the loading display.</p><h2 id="basic-usage" tabindex="-1">Basic Usage <a class="header-anchor" href="#basic-usage" aria-label="Permalink to &quot;Basic Usage&quot;">​</a></h2><p><code>TuiLoaderPanel</code> is typically used as the root container for cards or modules. Manage the component&#39;s lifecycle states by controlling the <code>loading</code> and <code>inited</code> properties.</p><p>::: demo A comprehensive demonstration including basic toggling, asynchronous simulation, and opacity configuration. loader-panel/index :::</p><blockquote><p><strong>Note</strong>: The above demonstration integrates the core features of <code>TuiLoaderPanel</code>, including click-through protection during loading (<code>pointer-events: none</code>) and the counter response when the <code>inited</code> state changes.</p></blockquote><h2 id="api-reference" tabindex="-1">API Reference <a class="header-anchor" href="#api-reference" aria-label="Permalink to &quot;API Reference&quot;">​</a></h2><h3 id="props" tabindex="-1">Props <a class="header-anchor" href="#props" aria-label="Permalink to &quot;Props&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Property</th><th>Description</th><th>Type</th><th>Default</th></tr></thead><tbody><tr><td><strong>loading</strong></td><td>Whether it is in a loading state. When <code>true</code>, the content area transparency is reduced, interaction is disabled, and the loading animation is displayed.</td><td><code>Boolean</code></td><td><code>true</code></td></tr><tr><td><strong>inited</strong></td><td>Initialization completion flag. When this value changes from <code>false</code> to <code>true</code>, it triggers an increment in the <code>portletCounter</code> within the global state.</td><td><code>Boolean</code></td><td><code>false</code></td></tr><tr><td><strong>loadingOpacity</strong></td><td>The opacity of the content area in the loading state. Range <code>0-1</code>; the smaller the value, the more transparent.</td><td><code>Number</code></td><td><code>0.1</code></td></tr></tbody></table><h3 id="slots" tabindex="-1">Slots <a class="header-anchor" href="#slots" aria-label="Permalink to &quot;Slots&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Slot Name</th><th>Description</th></tr></thead><tbody><tr><td><strong>default</strong></td><td>Default slot, used to place specific business component content.</td></tr></tbody></table><h2 id="core-mechanism-description" tabindex="-1">Core Mechanism Description <a class="header-anchor" href="#core-mechanism-description" aria-label="Permalink to &quot;Core Mechanism Description&quot;">​</a></h2><h3 id="interaction-blocking-and-visual-feedback" tabindex="-1">Interaction Blocking and Visual Feedback <a class="header-anchor" href="#interaction-blocking-and-visual-feedback" aria-label="Permalink to &quot;Interaction Blocking and Visual Feedback&quot;">​</a></h3><p>When <code>loading</code> is <code>true</code>, the component automatically applies the following styling behaviors:</p><ul><li><strong>Interaction Blocking</strong>: Sets <code>pointer-events: none</code> to prevent users from clicking or interacting with DOM elements that have not finished loading.</li><li><strong>Transparency Processing</strong>: The opacity of the content area is set to the value of <code>loadingOpacity</code> (default 0.1) to visually distinguish between &quot;loading&quot; and &quot;available&quot; states.</li></ul><h3 id="global-counter-integration-portlet-counter" tabindex="-1">Global Counter Integration (Portlet Counter) <a class="header-anchor" href="#global-counter-integration-portlet-counter" aria-label="Permalink to &quot;Global Counter Integration (Portlet Counter)&quot;">​</a></h3><p>This component interacts with the <code>$globalState/portletCounter</code> in the system&#39;s global state.</p><ul><li><strong>Trigger Timing</strong>: Monitors changes to the <code>inited</code> property.</li><li><strong>Execution Logic</strong>: Only when <code>inited</code> becomes <code>true</code>, it executes <code>$tState.globalState.portletCounter++</code>. This parameter can be monitored via the <code>$gPortletCounter</code> computed property.</li><li><strong>Use Case</strong>: Typically used in Dashboards or complex pages where the parent level monitors the total value of the counter to determine if all sub-modules have finished loading (e.g., <code>Total Modules = 5</code>, wait until <code>Counter === 5</code> to remove the page-level mask).</li></ul><h3 id="loading-effect-implementation-component-reuse" tabindex="-1">Loading Effect Implementation (Component Reuse) <a class="header-anchor" href="#loading-effect-implementation-component-reuse" aria-label="Permalink to &quot;Loading Effect Implementation (Component Reuse)&quot;">​</a></h3><p>Internally, this component calls the <strong><code>TuiLoadingArea</code></strong> component to achieve the area masking effect.</p><ul><li><strong>Separation of Concerns</strong>: <code>TuiLoaderPanel</code> is responsible for container layout, interaction logic (such as <code>pointer-events</code>), and global counter states, while <code>TuiLoadingArea</code> focuses on the visual rendering of the loading animation.</li><li><strong>Prop Pass-through</strong>: The component passes the received <code>loading</code> property directly to the internal <code>TuiLoadingArea</code> to ensure visual feedback remains consistent with the logical state.</li></ul><div class="tip custom-block"><p class="custom-block-title">Internal Dependency</p><p><code>TuiLoaderPanel</code> relies on the <strong><code>TuiLoadingArea</code></strong> component to display the loading animation. Please ensure <code>TuiLoadingArea</code> is registered or referenced in your project before use.</p></div>',23))])}const v=a(c,[["render",h]]);export{y as __pageData,v as default};
